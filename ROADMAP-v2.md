# AgentMonitor 产品路线图 v2.0

> **产品路线**：调试专家（独家差异化）+ 轻量评测能力
> **目标用户**：AI Agent 开发者、Prompt 工程师、QA 测试人员  
> **核心主张**：让 Agent 开发可调试、可评测、可迭代  
> **文档日期**：2026-04-26

---

## 目录

- [1. 产品定位](#1-产品定位)
- [2. 当前完成度](#2-当前完成度)
- [3. 路线图总览](#3-路线图总览)
- [4. Phase 1：测评体验补全（v1.1）](#4-phase-1测评体验补全v11)
- [5. Phase 2：自动化闭环（v1.2）](#5-phase-2自动化闭环v12)
- [6. Phase 3：调试深化（v1.3）](#6-phase-3调试深化v13)
- [7. 长期规划（v2.0+）](#7-长期规划v20)
- [8. 成功指标](#8-成功指标)

---

## 1. 产品定位

### 1.1 一句话定位

**AgentMonitor = Agent 的调试器 + 轻量评测平台**

- **调试器**：独家能力，解决 Agent "黑盒运行、出问题难定位"的痛点
- **轻量评测**：提供覆盖数据集、评估器、实验、报告的完整测评闭环，走"快速接入、开箱即用"路线

### 1.2 产品核心特性

| 维度 | 设计取向 |
|------|---------|
| **核心卖点** | 断点调试 + 轻量回归测试 |
| **使用时机** | 开发调试 + 上线前快速验证 + 线上问题排查 |
| **接入方式** | SDK 接入（框架无关、模型无关） |
| **评测深度** | 轻（预置规则、一键回归、自动对比） |
| **调试能力** | 断点、单步、变量检查、快照 |
| **部署门槛** | SQLite 开箱即用，5 分钟跑起来 |

### 1.3 目标用户分层

| 用户类型 | 核心诉求 | 主要使用模块 |
|---------|---------|------------|
| **独立开发者** | 快速调试 Agent，低成本验证效果 | 调试中心 + Playground + 轻量实验 |
| **中小团队** | 团队协作评测 Agent 质量，上线前把关 | 评测中心 + 观测中心 + Prompt 管理 |
| **企业运维** | 线上监控、问题告警、故障排查 | 观测中心（Dashboard + 告警）+ 调试中心 |
| **Prompt 工程师** | Prompt 版本迭代、效果验证、A/B 对比 | 提示工程 + 评测实验 + 报告对比 |

---

## 2. 当前完成度

### 2.1 已完成模块（v1.0）

```
✅ 观测中心
   ├── Dashboard（实时指标、趋势图表、连接状态）
   ├── Traces（调用链路、类型筛选、子调用树、详情面板）
   ├── Sessions（会话列表、Timeline/Chat/Unified 三视图）
   └── Alerts（延迟/错误率/成本告警）

✅ 评测中心
   ├── Datasets（CRUD、动态列、批量导入、版本管理 commit/rollback）
   ├── Reflow（Session 回流、Traces 回流）
   ├── Evaluators（5 种类型、版本管理、回滚）
   ├── Experiments（创建/启动/完成、报告、Bad Cases、横向对比）
   └── Results（结果上报、统计计算）

✅ 提示工程
   ├── Prompts（CRUD、版本历史、diff 对比、回滚）
   ├── Playground（变量注入、运行、多模型对比）
   └── Model Configs（参数管理）

✅ 调试中心
   ├── Breakpoints（断点设置、条件触发）
   ├── Snapshots（状态快照、历史回放）
   └── Variables（变量检查、实时查看）

✅ 系统
   ├── Auth（注册/登录/Token）
   ├── Projects（项目创建/切换/隔离）
   └── API Keys（生成/撤销）
```

### 2.2 已具备的基础能力

| 能力模块 | 完成状态 |
|-------------|------------|
| 数据集管理 | ✅ 已完整实现（含版本、回流） |
| 评估器管理 | ✅ 已支持 5 种基础类型 |
| 实验系统 | ✅ 已跑通全流程（创建→运行→报告→对比） |
| Prompt 版本管理 | ✅ 已实现（版本、diff、回滚） |
| Playground | ✅ 已实现（变量注入、多模型对比） |
| Trace 追踪 | ✅ 已实现（链路、筛选、详情） |
| Session 回放 | ✅ 已实现（三种视图） |
| 数据回流 | ✅ 已实现（Session + Traces 双通道） |

### 2.3 尚未补齐的差距

| 差距维度 | 具体能力 | 优先级 |
|---------|---------|--------|
| **评测体验** | 预置评估器模板、LLM Judge 体验优化、实验执行引导 | P0 |
| **自动化** | Trace 自动评测、人工校准、Prompt 改动自动回归 | P1 |
| **调试深化** | 条件断点、Session 回放调试、ReAct 可视化 | P1 |
| **协作** | 团队空间、权限管理、分享链接 | P2 |
| **标准化** | OpenTelemetry Span 规范、更细粒度追踪 | P2 |

---

## 3. 路线图总览

```
2026-Q2                    2026-Q3                    2026-Q4
  │                          │                          │
  ▼                          ▼                          ▼
┌─────────────┐        ┌─────────────┐        ┌─────────────┐
│  Phase 1    │   →    │  Phase 2    │   →    │  Phase 3    │
│  v1.1       │        │  v1.2       │        │  v1.3       │
│             │        │             │        │             │
│ 测评体验    │        │ 自动化闭环  │        │ 调试深化    │
│ 补全        │        │ 构建        │        │ + 标准化    │
└─────────────┘        └─────────────┘        └─────────────┘
     │                       │                       │
     ▼                       ▼                       ▼
预置评估器模板           Trace 自动评测           条件断点
LLM Judge 体验优化       人工校准流程             Session 回放调试
实验执行引导             Prompt 自动回归          ReAct 可视化
                       团队空间初版               OpenTelemetry
```

---

## 4. Phase 1：测评体验补全（v1.1）

**目标**：让评测功能从"能跑"变成"好用"，降低用户第一次做实验的门槛。

**时间**：4-6 周  
**核心用户价值**：一个从未用过评测系统的新用户，能在 10 分钟内完成第一个实验。

### 4.1 预置评估器模板

**问题**：当前创建评估器需要用户手动选择类型、填写配置参数，对新手不友好。

**方案**：

| 场景 | 自动推荐评估器组合 |
|------|------------------|
| 数据集类型 = `qa` | 自动创建「准确性评估器（exact_match）」+「完整性评估器（contains）」 |
| 数据集类型 = `chat` | 自动创建「语义相似度（similarity, threshold=0.8）」+「安全性检测（regex 黑名单）」 |
| 数据集类型 = `custom` | 创建「精确匹配」+「LLM Judge（criteria=准确性）」 |

**验收标准**：
- 创建数据集时，根据类型自动预置 2-3 个评估器
- 用户可一键启用/禁用预置评估器
- 预置评估器支持自定义修改参数

### 4.2 LLM Judge 体验优化

**问题**：当前 llm_judge 类型需要用户手写 criteria 和选择模型，使用门槛高。

**方案**：

| 优化点 | 具体内容 |
|--------|---------|
| **预置 Criteria 模板** | 准确性、完整性、安全性、简洁性、礼貌度、结构化程度 |
| **评分维度选择** | 用户勾选维度，系统自动生成评判 Prompt |
| **模型选择** | 下拉选择评判模型（gpt-4 / claude-3 / 自定义） |
| **评分方式** | 1-5 分制或 0-1 分制可选 |

**验收标准**：
- 创建 LLM Judge 评估器时，Criteria 支持下拉选择而非手写
- 评判结果展示时，显示各维度的分项得分
- 支持批量重评（修改 criteria 后，对已有结果重新评判）

### 4.3 实验执行引导

**问题**：当前实验创建后，用户不知道如何"跑"实验——需要写脚本遍历数据集、调用 Agent、回传结果。

**方案**：

| 功能 | 说明 |
|------|------|
| **执行脚本模板** | 实验详情页提供 Python/TypeScript 脚本模板，用户填入 Agent 调用函数即可运行 |
| **进度追踪** | 实验运行中显示"已执行 X/Y 条用例"的进度条 |
| **实时结果** | 每上传一条 Result，报告实时更新（无需等全部跑完） |
| **快速测试** | 支持「单条试运行」：选一条用例，在 Playground 中快速验证 |

**验收标准**：
- 实验详情页有「下载执行脚本」按钮
- 脚本模板包含：遍历数据集 → 调用 Agent → 评估打分 → 上传结果的完整代码
- 实验状态从 running 开始，实时显示已上传结果数 / 总用例数

### 4.4 Phase 1 验收清单

- [ ] 创建 qa/chat/custom 数据集时，自动预置对应评估器
- [ ] LLM Judge 支持 6 种预置 Criteria 模板
- [ ] 实验详情页提供执行脚本下载（Python + TypeScript）
- [ ] 实验运行中实时显示进度和结果
- [ ] 新用户从注册到完成第一个实验，操作步骤 ≤ 8 步

---

## 5. Phase 2：自动化闭环（v1.2）

**目标**：构建"线上观测 → 自动评测 → 发现问题 → 优化验证"的数据飞轮。

**时间**：6-8 周  
**核心用户价值**：用户不需要手动跑实验，系统持续自动评估 Agent 质量，发现问题主动通知。

### 5.1 Trace 自动评测

**问题**：当前评测依赖人工触发实验，无法持续监控线上质量变化。

**方案**：

| 功能 | 说明 |
|------|------|
| **自动评测任务** | 创建定时任务：每隔 N 小时，采样最近 M 条 Trace，自动跑评估 |
| **评估规则配置** | 选择评估器 + 筛选条件（trace_type = llm, status = success） |
| **趋势报告** | 生成「质量趋势图」：通过率随时间的变化曲线 |
| **退化告警** | 当自动评测通过率比上一周期下降 > X% 时，触发告警 |

**使用场景**：
- 每天凌晨自动评测昨日 10% 的 LLM Trace
- 周一早上收到周报：本周平均通过率 87%，比上周下降 3%，主要退化在「售后场景」

**验收标准**：
- 支持创建/编辑/删除自动评测任务
- 任务执行后自动生成趋势报告
- 支持邮件/Webhook 通知退化告警

### 5.2 人工校准流程

**问题**：自动评测可能误判（尤其是 LLM Judge），需要人工修正来提高评估准确性。

**方案**：

| 功能 | 说明 |
|------|------|
| **校准模式** | 在 Bad Cases 页面，每条结果增加「✅ 正确 / ❌ 确实失败」按钮 |
| **校准记录** | 记录谁校准的、校准时间、原始结果 vs 校准后结果 |
| **校准后重算** | 校准完成后，自动重新计算实验报告（通过率、平均分更新） |
| **校准统计** | 展示评估器的「误判率」（被人工校准的比例） |

**验收标准**：
- Bad Cases 页面支持单条校准
- 支持批量校准（勾选多条一键标记）
- 校准后实验报告实时更新
- 展示评估器误判率统计

### 5.3 Prompt 改动自动回归

**问题**：Prompt 修改后，开发者经常忘记跑实验验证，导致线上退化。

**方案**：

| 功能 | 说明 |
|------|------|
| **保存时选项** | Prompt 保存新版本时，勾选「自动创建回归实验」 |
| **自动实验** | 系统用新 Prompt 自动跑指定数据集（需要用户提供 Agent 调用入口） |
| **对比报告** | 实验完成后，自动生成「新版本 vs 当前版本」的对比报告 |
| **回归告警** | 如果新版本通过率低于当前版本，发送告警通知 |

**验收标准**：
- Prompt 保存时支持勾选「自动回归测试」
- 自动生成对比实验和报告
- 新版本效果更差时，主动告警提示

### 5.4 Phase 2 验收清单

- [ ] 支持创建定时自动评测任务
- [ ] 自动生成质量趋势报告（按日/周/月）
- [ ] Bad Cases 支持人工校准（单条 + 批量）
- [ ] 校准后自动重算实验报告
- [ ] Prompt 保存新版本时，可选自动回归测试
- [ ] 自动生成新旧版本对比报告

---

## 6. Phase 3：调试深化（v1.3）

**目标**：把断点调试从"能断"升级为"专业级调试工具"，巩固独家优势。

**时间**：6-8 周  
**核心用户价值**：开发者能像调试传统程序一样调试 Agent，精准定位每一轮 Thought/Action/Observation 的问题。

### 6.1 条件断点

**问题**：当前断点是"无条件触发"，复杂场景下频繁中断影响效率。

**方案**：

| 断点类型 | 说明 |
|---------|------|
| **命中次数断点** | 命中 N 次后才触发（如第 5 轮对话才断） |
| **表达式断点** | 变量满足条件时触发（如 `latency > 2000`） |
| **内容匹配断点** | 消息内容包含特定关键词时触发（如包含"退款"） |
| **错误断点** | 只在异常时触发（工具调用失败、LLM 返回格式错误） |

**验收标准**：
- 支持设置命中次数条件
- 支持基于变量值的表达式条件
- 支持基于内容的关键词匹配条件

### 6.2 Session 回放调试

**问题**：线上出了问题，开发者想复现当时的执行过程，但只能看日志不能介入。

**方案**：

| 功能 | 说明 |
|------|------|
| **历史 Session 重放** | 选择历史 Session，以调试模式重新执行 |
| **逐步重放** | 按原始时间线逐轮重放，每轮可检查状态 |
| **差异对比** | 重放结果与原始结果对比，发现不一致（如模型更新导致输出变化） |
| **修改后重放** | 修改 Prompt 后，用同一个 Session 重新运行，对比效果 |

**验收标准**：
- 支持选择任意历史 Session 进入回放调试模式
- 重放过程中可设置断点、检查变量
- 支持「原始结果」与「重放结果」的 diff 对比

### 6.3 ReAct 可视化

**问题**：ReAct / Multi-Agent 的执行流程是链式的，纯文本 Trace 难以直观理解。

**方案**：

| 功能 | 说明 |
|------|------|
| **循环图** | 把 Thought → Action → Observation → Thought 的循环可视化为流程图 |
| **状态机视图** | 展示 Agent 在不同状态间的跳转 |
| **决策分支** | 展示 Agent 面临多个选项时的决策路径 |
| **时间轴** | 每个循环步骤的时间消耗可视化 |

**验收标准**：
- Session 详情页支持切换为「ReAct 流程图」视图
- 流程图节点可点击查看详情（输入、输出、延迟）
- 失败节点用红色高亮，成功节点用绿色

### 6.4 OpenTelemetry 兼容

**问题**：当前 Trace 结构是自定的，与行业标准不兼容，难以集成到现有可观测性体系。

**方案**：

| 功能 | 说明 |
|------|------|
| **Span 规范** | Trace 结构对齐 OpenTelemetry Span 规范（trace_id, span_id, parent_span_id） |
| **嵌套 Span** | 支持多层嵌套 Span，精确表达调用层级 |
| **属性标准化** | 使用 OTel 标准属性名（如 `llm.model`, `llm.token_count`） |
| **Exporter** | 支持导出到 Jaeger / Zipkin / 其他 OTel Collector |

**验收标准**：
- Trace 数据结构兼容 OpenTelemetry Span 规范
- 支持导出标准 OTel 格式的 trace 数据
- 可在 Jaeger 等第三方工具中查看 AgentMonitor 的 Trace

### 6.5 Phase 3 验收清单

- [ ] 支持条件断点（命中次数、表达式、内容匹配、错误）
- [ ] 支持历史 Session 回放调试
- [ ] 支持 ReAct 流程图可视化
- [ ] Trace 数据结构兼容 OpenTelemetry
- [ ] 支持导出到 Jaeger / Zipkin

---

## 7. 长期规划（v2.0+）

### 7.1 AI 辅助优化

| 功能 | 说明 |
|------|------|
| **Bad Case 自动归因** | 分析 Bad Cases 的共性特征，自动归类（如"所有涉及退款的 case 都失败"） |
| **Prompt 自动优化建议** | 基于 Bad Cases，用 LLM 生成 Prompt 改进建议 |
| **评测集自动扩充** | 基于现有用例，用 LLM 生成相似变体，扩充覆盖 |

### 7.2 团队协作

| 功能 | 说明 |
|------|------|
| **团队空间** | 支持创建团队，邀请成员，分配角色 |
| **权限管理** | 管理员/开发者/观察员三级权限 |
| **分享链接** | 实验报告、Session 回放支持生成分享链接 |
| **评论与审批** | Prompt 新版本可提交审批，团队成员可评论 |

### 7.3 多环境管理

| 功能 | 说明 |
|------|------|
| **环境隔离** | 支持 Dev / Staging / Prod 三套环境数据隔离 |
| **环境对比** | 对比同一 Agent 在不同环境的表现差异 |
| **灰度发布** | 支持按流量比例切换 Prompt/模型版本 |

---

## 8. 成功指标

### 8.1 功能完成度指标

| 阶段 | 指标 | 目标值 |
|------|------|--------|
| Phase 1 结束 | 新用户完成首个实验的平均时间 | ≤ 10 分钟 |
| Phase 1 结束 | 预置评估器使用率 | ≥ 70% |
| Phase 2 结束 | 自动评测任务创建数 / 项目 | ≥ 1 |
| Phase 2 结束 | 人工校准使用率 | ≥ 30% |
| Phase 3 结束 | 条件断点使用率 | ≥ 20% |
| Phase 3 结束 | OTel 导出功能使用率 | ≥ 15% |

### 8.2 用户价值指标

| 指标 | 说明 | 目标 |
|------|------|------|
| **首次实验成功率** | 新用户第一次创建实验并成功运行的比例 | ≥ 80% |
| **实验 rerun 率** | 一个实验被重复运行（回归测试）的平均次数 | ≥ 3 次/月 |
| **Bad Case 修复率** | 被标记的 Bad Case 在 7 天内被修复的比例 | ≥ 60% |
| **调试使用频次** | 每个项目每周使用断点调试的平均次数 | ≥ 5 次 |
| **Prompt 版本数** | 每个 Prompt 的平均版本数量 | ≥ 3 个 |

### 8.3 能力成熟度目标

| 维度 | 当前 | Phase 1 | Phase 2 |
|------|------|---------|---------|
| 评测开箱即用度 | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 自动化程度 | ⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| 调试专业度 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 观测完整度 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 整体成熟度 | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

---

> **文档结束**
>
> 本路线图按季度滚动更新，每阶段结束后根据实际完成情况和用户反馈调整下一阶段优先级。
