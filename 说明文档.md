# AgentMonitor 项目说明文档

## 一、项目规划

### 1.1 项目目标
基于《需求文档-全面参考同类平台.md》，开发一款具备企业级 B 端体验的 AI Agent 监控运维平台（AgentMonitor v2.0）。全面参考 同类平台 同类平台，重点强化“Agent 调试专家”的独家优势，降低接入成本，并提供更好用的体验。

### 1.2 阶段目标
- **阶段一（当前）**：基于需求文档完成高保真原型设计（UI 侧），确立核心视觉规范（极简科技风、深色护眼模式、#165DFF主色等）。
- **阶段二**：核心模块开发（Prompt 工程、评测中心、观测中心增强）。
- **阶段三**：调试中心、团队协作与数据回流能力建设。
- **阶段四**：插件生态与 AI 辅助增强。

---

## 二、实施方案

### 2.1 视觉与交互规范
- **整体风格**：企业级 B 端 UI 设计，参考同类平台，极简专业科技风，数据优先，低视觉噪音，高信息密度。
- **色彩体系**：深色护眼模式（背景色 `#0A0B10`，卡片色 `#13141F`），主色 `#165DFF`，告警标准色（成功 `#00B42A`，失败/错误 `#F53F3F`）。
- **材质与排版**：微磨砂玻璃拟态，轻拟态卡片，极柔投影，线性统一图标，无衬线黑体，等宽代码字体。
- **分辨率与输出**：1920*1080 基础分辨率，2x 高清导出，高保真无水印，符合 Figma 规范。

### 2.2 技术实施
- 使用 MCP `pencil` 插件系统构建高保真 UI 原型，确保界面符合结构化与参数化设计标准。

---

## 三、进度记录

### 【已完成】UI 原型设计与视觉规范确立
- [x] **任务 1**：解析《需求文档-全面参考同类平台.md》中的观测中心与面板需求。
  - **完成时间**：2026-04-24
  - **结果说明**：已完成文档分析，确定仪表盘（Dashboard）作为首个设计的核心页面。
- [x] **任务 2**：使用 Pencil 工具完成高保真 UI 设计图绘制。
  - **完成时间**：2026-04-24
  - **结果说明**：已完成 1920*1080 分辨率的高保真设计稿绘制。包含了侧边栏导航、顶部环境与用户信息、核心指标看板（四项数据统计及趋势分析）、柱状图趋势展示、以及最近 Trace 调用明细表。设计严格遵循了深色护眼模式、#165DFF 主色以及运维标准色彩（绿/红）的规范。
- [x] **任务 3**：导出无水印高保真设计图与 .pen 工程文件。
  - **完成时间**：2026-04-24
  - **结果说明**：已成功导出 2x 缩放比例的高清无水印 PNG 图像及对应的 Pencil 原生源文件，存放于 `docs/AgentMonitor-v2-Dashboard.png` 与 `docs/AgentMonitor-v2-Dashboard.pen`。

### 【已完成】P0-01 Span 级分布式追踪 + Trace 调用链可视化
- [x] **任务 4**：后端数据库迁移与 Span Service 实现。
  - **完成时间**：2026-04-29
  - **结果说明**：完成 `spans` 表创建（SQLite + PostgreSQL 双迁移文件），实现 `span.ts` Service（createSpan、getSpanTree、循环检测、stats 计算），新增 `sanitize.ts` 中间件（敏感字段过滤）和 `truncate.ts` 工具（大对象截断）。后端测试 473 passed。
- [x] **任务 5**：SDK Span API 实现（TypeScript/Python/Go）。
  - **完成时间**：2026-04-29
  - **结果说明**：SDK TypeScript 实现 startSpan/endSpan/withSpan/spanStack/bufferSpan，支持 trace() 双写。Python 和 Go SDK 同步实现 Span 相关代码。SDK 测试全部通过。
- [x] **任务 6**：前端可视化组件开发（Gantt 图 + Span 树 + 详情面板）。
  - **完成时间**：2026-04-30
  - **结果说明**：完成 `useSpanTree` Hook、`SpanTreeList`（可折叠树列表）、`GanttChart` + `SpanBar`（时间轴图）、`SpanDetailPanel`（Token/Cost 详情）。新增 `TraceDetailPage` 页面，注册 `/traces/:id` 路由，TracesPage 列表点击跳转。前端构建成功，TypeScript 编译无错误。
- [x] **任务 7**：观测中心国际化收尾（Trace 详情页与列表页）。
  - **完成时间**：2026-04-30
  - **结果说明**：完成 `TraceDetailPage`、`SpanTreeList`、`GanttChart`、`SpanDetailPanel` 与 `TracesPage` 的硬编码文案替换，统一接入 `i18n`。补充多组中英文翻译键（评测状态、评测弹窗、回流弹窗、Span 标签）。修复翻译键重复冲突后，前端构建再次通过（`npm run build`）。
- [x] **任务 8**：评测中心与 Playground 国际化收尾。
  - **完成时间**：2026-04-30
  - **结果说明**：完成 `EvaluationPage` 任务页、实验进度区、实验报告弹窗、任务创建弹窗、评估器模板区域的硬编码文案替换，统一改为 `i18n` 键。新增翻译键：`presetTemplate`、`selectPresetTemplate`、`autoCreatePresetEvaluators`、`calibratedAfter`、`calibratedSummary`。并确认 `DashboardPage` 与 `PromptsPage`（含 Playground）无中文硬编码残留。前端构建通过（`npm run build`）。
- [x] **任务 9**：告警中心国际化补全与残留扫描。
  - **完成时间**：2026-04-30
  - **结果说明**：完成 `AlertsPage` 历史摘要、证据信息、阈值与关联调用区域的硬编码文案替换，统一改为 `i18n` 键。新增翻译键：`linkedTrace`、`errorRate`。并确认 `DebuggingPage`、`SessionsPage` 无中文硬编码残留。前端构建通过（`npm run build`）。
- [x] **任务 10**：登录注册与设置链路国际化最终清零。
  - **完成时间**：2026-05-07
  - **结果说明**：完成 `LoginPage`、`RegisterPage`、`SettingsPage`、`ModelConfigsPage`、`SessionDetailPage`、`TraceDetailPage` 六页面最终扫描。修复登录/注册页语言切换按钮硬编码（`EN/中文`），改为 `i18n` 键 `langEn/langZh`。前端构建通过（`npm run build`），关键文件诊断无错误。
- [x] **任务 11**：英文硬编码最终收敛（用户可见文案）。
  - **完成时间**：2026-05-07
  - **结果说明**：完成 `FeedbackPage` 全页英文文案国际化替换，修复 `RegisterPage` 注册错误提示英文硬编码，修复 `ProtectedRoute` 加载态英文硬编码。新增 `feedbackTitle`、`feedbackSubtitle`、`totalFeedback`、`allRatings`、`ratingPositive`、`sessionLabel`、`passwordsNoMatch` 等中英文翻译键。前端构建通过（`npm run build`），相关文件诊断无错误。
- [x] **任务 12**：严格模式国际化扫尾（示例占位符与单位文案）。
  - **完成时间**：2026-05-07
  - **结果说明**：完成 `TracesPage`、`AlertsPage`、`ModelConfigsPage`、`PromptsPage`、`LoginPage`、`RegisterPage` 的示例占位符与固定单位文案替换，统一改为 `i18n` 键。新增翻译键：`evaluatorExample`、`matchedJsonExample`、`datasetNameExample`、`datasetItemsSuffix`、`alertConditionExample`、`modelNameExample`、`modelIdExample`、`apiKeyExample`、`baseUrlExample`、`playgroundInputPlaceholder`、`modelListExample`、`nameExample`、`emailExample`。前端构建通过（`npm run build`），相关文件诊断无错误。
- [x] **任务 13**：全仓 i18n 最终审计与清零。
  - **完成时间**：2026-05-07
  - **结果说明**：对全仓前端代码进行最终审计扫描。修复最后两处残留：1) `ModelConfigsPage`/`PromptsPage` 中 `Custom` provider 选项显示文本改用 `t.custom`；2) `TracesPage` 中 `alert('Successfully added to dataset')` 硬编码改为 `t.exportDatasetSuccess`。所有占位符（placeholder）、alert/setError 错误提示、option 选项显示文本均已统一国际化。品牌名（OpenAI/Anthropic/Google 等）、模型名（gpt-4/claude-3-opus 等）、评估器类型标识符（exact_match/contains 等）作为技术标识符保留不译。前端构建通过（`npm run build`），全仓 grep 确认无遗漏。
- [x] **任务 14**：完善 `.gitignore`，排除不需要提交到仓库的文件与目录。
  - **完成时间**：2026-05-08
  - **结果说明**：新增忽略项：`.trae/`（Trae IDE 工具元数据）、`plans/`（内部规划文档）、`extract.py`（一次性临时脚本）。原有规则已覆盖 `node_modules`、`dist`、`.env`、数据库文件、IDE 配置、测试产物等。确认被忽略的文件不再出现在 `git status` 中。

### 【已完成】本地开发部署链路打通
- [x] **任务 15**：补齐本地开发默认环境变量与健康检查验证。
  - **完成时间**：2026-05-24
  - **结果说明**：新增 `backend/.env`（SQLite 默认 + 调高开发环境限流阈值）与 `frontend/.env`（VITE_API_URL 指向本地后端）。本地启动后端与前端服务并验证 `/health` 返回 200。
