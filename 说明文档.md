# AgentMonitor 项目说明文档

## 一、项目规划

### 1.1 项目目标

开发一款具备企业级 B 端体验的 AI Agent 监控运维平台（AgentMonitor v2.0）。重点强化“Agent 调试专家”的独家优势，降低接入成本，并提供更好用的体验。

### 1.2 阶段目标

- **阶段一（当前）**：基于需求文档完成高保真原型设计（UI 侧），确立核心视觉规范（极简科技风、深色护眼模式、#165DFF主色等）。

- **阶段二**：核心模块开发（Prompt 工程、评测中心、观测中心增强）。

- **阶段三**：调试中心、团队协作与数据回流能力建设。

- **阶段四**：插件生态与 AI 辅助增强。

***

## 二、实施方案

### 2.1 视觉与交互规范

- **整体风格**：企业级 B 端 UI 设计，极简专业科技风，数据优先，低视觉噪音，高信息密度。

- **色彩体系**：深色护眼模式（背景色 `#0A0B10`，卡片色 `#13141F`），主色 `#165DFF`，告警标准色（成功 `#00B42A`，失败/错误 `#F53F3F`）。

- **材质与排版**：微磨砂玻璃拟态，轻拟态卡片，极柔投影，线性统一图标，无衬线黑体，等宽代码字体。

- **分辨率与输出**：1920\*1080 基础分辨率，2x 高清导出，高保真无水印，符合 Figma 规范。

### 2.2 技术实施

- 使用 MCP `pencil` 插件系统构建高保真 UI 原型，确保界面符合结构化与参数化设计标准。

***

## 三、进度记录

### 【已完成】UI 原型设计与视觉规范确立

- [x] **任务 1**：梳理观测中心与面板需求。
  - **完成时间**：2026-04-24

  - **结果说明**：已完成文档分析，确定仪表盘（Dashboard）作为首个设计的核心页面。

- [x] **任务 2**：使用 Pencil 工具完成高保真 UI 设计图绘制。
  - **完成时间**：2026-04-24

  - **结果说明**：已完成 1920\*1080 分辨率的高保真设计稿绘制。包含了侧边栏导航、顶部环境与用户信息、核心指标看板（四项数据统计及趋势分析）、柱状图趋势展示、以及最近 Trace 调用明细表。设计严格遵循了深色护眼模式、#165DFF 主色以及运维标准色彩（绿/红）的规范。

- [x] **任务 3**：导出无水印高保真设计图与 .pen 工程文件。
  - **完成时间**：2026-04-24

  - **结果说明**：已成功导出 2x 缩放比例的高清无水印 PNG 图像及对应的 Pencil 原生源文件，存放于 `docs/AgentMonitor-v2-Dashboard.png` 与 `docs/AgentMonitor-v2-Dashboard.pen`。

### 【已完成】P0-01 Span 级分布式追踪 + Trace 调用链可视化

- [x] **任务 4**：后端数据库迁移与 Span Service 实现。
  - **完成时间**：2026-04-29

  - **结果说明**：完成 `spans` 表创建（SQLite + PostgreSQL 双迁移文件），实现 `span.ts` Service（createSpan、getSpanTree、循环检测、stats 计算），新增 `sanitize.ts` 中间件（敏感字段过滤）和 `truncate.ts` 工具（大对象截断）。后端测试 473 passed。

- [x] **任务 5**：SDK Span API 实现（TypeScript/Python/Go）。
  - **完成时间**：2026-04-29

  - **结果说明**：SDK TypeScript 实现 startSpan/endSpan/withSpan/spanStack/bufferSpan，支持 trace() 双写。Python 和 Go SDK 同步实现 Span 相关代码。SDK 测试全部通过。

- [x] **任务 6**：前端可视化组件开发（Gantt 图 + Span 树 + 详情面板）。
  - **完成时间**：2026-04-30

  - **结果说明**：完成 `useSpanTree` Hook、`SpanTreeList`（可折叠树列表）、`GanttChart` + `SpanBar`（时间轴图）、`SpanDetailPanel`（Token/Cost 详情）。新增 `TraceDetailPage` 页面，注册 `/traces/:id` 路由，TracesPage 列表点击跳转。前端构建成功，TypeScript 编译无错误。

- [x] **任务 7**：观测中心国际化收尾（Trace 详情页与列表页）。
  - **完成时间**：2026-04-30

  - **结果说明**：完成 `TraceDetailPage`、`SpanTreeList`、`GanttChart`、`SpanDetailPanel` 与 `TracesPage` 的硬编码文案替换，统一接入 `i18n`。补充多组中英文翻译键（评测状态、评测弹窗、回流弹窗、Span 标签）。修复翻译键重复冲突后，前端构建再次通过（`npm run build`）。

- [x] **任务 8**：评测中心与 Playground 国际化收尾。
  - **完成时间**：2026-04-30

  - **结果说明**：完成 `EvaluationPage` 任务页、实验进度区、实验报告弹窗、任务创建弹窗、评估器模板区域的硬编码文案替换，统一改为 `i18n` 键。新增翻译键：`presetTemplate`、`selectPresetTemplate`、`autoCreatePresetEvaluators`、`calibratedAfter`、`calibratedSummary`。并确认 `DashboardPage` 与 `PromptsPage`（含 Playground）无中文硬编码残留。前端构建通过（`npm run build`）。

- [x] **任务 9**：告警中心国际化补全与残留扫描。
  - **完成时间**：2026-04-30

  - **结果说明**：完成 `AlertsPage` 历史摘要、证据信息、阈值与关联调用区域的硬编码文案替换，统一改为 `i18n` 键。新增翻译键：`linkedTrace`、`errorRate`。并确认 `DebuggingPage`、`SessionsPage` 无中文硬编码残留。前端构建通过（`npm run build`）。

- [x] **任务 10**：登录注册与设置链路国际化最终清零。
  - **完成时间**：2026-05-07

  - **结果说明**：完成 `LoginPage`、`RegisterPage`、`SettingsPage`、`ModelConfigsPage`、`SessionDetailPage`、`TraceDetailPage` 六页面最终扫描。修复登录/注册页语言切换按钮硬编码（`EN/中文`），改为 `i18n` 键 `langEn/langZh`。前端构建通过（`npm run build`），关键文件诊断无错误。

- [x] **任务 11**：英文硬编码最终收敛（用户可见文案）。
  - **完成时间**：2026-05-07

  - **结果说明**：完成 `FeedbackPage` 全页英文文案国际化替换，修复 `RegisterPage` 注册错误提示英文硬编码，修复 `ProtectedRoute` 加载态英文硬编码。新增 `feedbackTitle`、`feedbackSubtitle`、`totalFeedback`、`allRatings`、`ratingPositive`、`sessionLabel`、`passwordsNoMatch` 等中英文翻译键。前端构建通过（`npm run build`），相关文件诊断无错误。

- [x] **任务 12**：严格模式国际化扫尾（示例占位符与单位文案）。
  - **完成时间**：2026-05-07

  - **结果说明**：完成 `TracesPage`、`AlertsPage`、`ModelConfigsPage`、`PromptsPage`、`LoginPage`、`RegisterPage` 的示例占位符与固定单位文案替换，统一改为 `i18n` 键。新增翻译键：`evaluatorExample`、`matchedJsonExample`、`datasetNameExample`、`datasetItemsSuffix`、`alertConditionExample`、`modelNameExample`、`modelIdExample`、`apiKeyExample`、`baseUrlExample`、`playgroundInputPlaceholder`、`modelListExample`、`nameExample`、`emailExample`。前端构建通过（`npm run build`），相关文件诊断无错误。

- [x] **任务 13**：全仓 i18n 最终审计与清零。
  - **完成时间**：2026-05-07

  - **结果说明**：对全仓前端代码进行最终审计扫描。修复最后两处残留：1) `ModelConfigsPage`/`PromptsPage` 中 `Custom` provider 选项显示文本改用 `t.custom`；2) `TracesPage` 中 `alert('Successfully added to dataset')` 硬编码改为 `t.exportDatasetSuccess`。所有占位符（placeholder）、alert/setError 错误提示、option 选项显示文本均已统一国际化。品牌名（OpenAI/Anthropic/Google 等）、模型名（gpt-4/claude-3-opus 等）、评估器类型标识符（exact\_match/contains 等）作为技术标识符保留不译。前端构建通过（`npm run build`），全仓 grep 确认无遗漏。

- [x] **任务 14**：完善 `.gitignore`，排除不需要提交到仓库的文件与目录。
  - **完成时间**：2026-05-08

  - **结果说明**：新增忽略项：`.trae/`（Trae IDE 工具元数据）、`plans/`（内部规划文档）、`extract.py`（一次性临时脚本）。原有规则已覆盖 `node_modules`、`dist`、`.env`、数据库文件、IDE 配置、测试产物等。确认被忽略的文件不再出现在 `git status` 中。

### 【已完成】本地开发部署链路打通

- [x] **任务 15**：补齐本地开发默认环境变量与健康检查验证。
  - **完成时间**：2026-05-24

  - **结果说明**：新增 `backend/.env`（SQLite 默认 + 调高开发环境限流阈值）与 `frontend/.env`（VITE\_API\_URL 指向本地后端）。本地启动后端与前端服务并验证 `/health` 返回 200。

### 【已完成】前端页面中英文混用局部修复

- [x] **任务 16**：修复快速评测向导、语言切换按钮与 Trace 树加载错误的中英文混用问题。
  - **完成时间**：2026-06-02

  - **结果说明**：完成 `EvalWizard` 三步向导的中英文分支文案切换，修复数据集条目单位 `items`、`Trace 数量` 等混用显示；移除 `LanguageToggle` 的硬编码 `🌐 EN/中文`，改为基于当前语言显示切换目标；`useSpanTree` 加载失败提示改为按语言输出。已对变更文件执行诊断检查且无新增错误。前端构建时发现项目当前还存在若干既有 TypeScript 错误（如 `FilterBar`、`Sidebar`、`TraceDetail`），未在本次范围内处理。

### 【已完成】模型配置测试连通性空请求修复

- [x] **任务 17**：修复“测试连通性”接口因空 JSON 请求体导致的 400 报错。
  - **完成时间**：2026-06-02

  - **结果说明**：定位到 `frontend/src/api.ts` 的通用请求封装会为所有请求无条件附加 `Content-Type: application/json`。当 `POST /model-configs/:id/test` 未携带 body 时，Fastify 会按空 JSON 请求处理并返回 `FST_ERR_CTP_EMPTY_JSON_BODY`。现已改为仅在存在请求体、且不是 `FormData`、且调用方未显式指定 `Content-Type` 时才附加 JSON 请求头。已检查 `frontend/src/api.ts`，无新增诊断错误。

### 【已完成】v2.0 技术方案 PR-01 至 PR-10

- [x] **任务 18**：PR-01 迁移基础设施。
  - **完成时间**：2026-08-24

  - **结果说明**：新增统一迁移注册表 `migration-registry.ts`，支持 SQLite/PostgreSQL 同编号迁移、事务执行与幂等台账；接入 SQLite 初始化与 PostgreSQL 显式迁移流程；补充迁移测试。

- [x] **任务 19**：PR-02 Telemetry V2 batch 入口与 Span/Trace Upsert。
  - **完成时间**：2026-08-24

  - **结果说明**：新增 `POST /api/v2/telemetry/batch`、Telemetry V2 JSON Schema 与示例；完成事件回执幂等、Span Upsert、Trace Upsert、V1 `/traces` 与 OTel 导入适配；修复 Trace Date 参数绑定问题；后端相关测试通过。

- [x] **任务 20**：PR-03 三语言 Context 与可靠队列验证。
  - **完成时间**：2026-08-24

  - **结果说明**：验证 TypeScript AsyncLocalStorage、Python ContextVar、Go context.Context 均已正确隔离并发上下文；分别新增 100 并发契约测试，覆盖 traceId/parentSpanId 不串扰。TypeScript SDK 35 测试、Python SDK 29 测试、Go SDK 测试全部通过。

- [x] **任务 21**：PR-04 Onboarding 与 Integration Health。
  - **完成时间**：2026-08-24

  - **结果说明**：新增 `GET /api/v2/projects/:projectId/integration-status`，聚合最近 60 分钟 Trace/Span，输出 connected、SDK 检测、Agent/LLM/Tool/Retrieval/Decision Hook 覆盖、字段完整度、诊断告警与示例 Trace；前端 Dashboard 新增 Integration Health 卡片并每 15 秒轮询。后端新增 3 个集成测试全部通过，前后端 TypeScript 编译通过。

- [x] **任务 22**：PR-05 Target 领域与 prompt\_model。
  - **完成时间**：2026-08-24

  - **结果说明**：新增 agent\_targets / agent\_target\_versions 两张表（SQLite 初始化 + p003\_target\_domain 迁移，PostgreSQL 同步），Target 主表项目内名称唯一，版本表 (target\_id, version\_number) 唯一且不可变。新增 agent-target 服务，支持 Target 与首个版本创建、列表、详情、版本列表、新建版本、版本详情与 prompt\_model test-invoke；prompt\_model 版本按 invocationConfig 中的 promptVersionId/promptId + modelConfigId 调用 callTargetModel，实现 PromptVersion 精确执行；ensureModelConfigTarget 将旧 ModelConfig 自动包装为兼容 Target。注册 `/api/v2/evaluation/targets` 与 `/api/v2/evaluation/target-versions` 路由并做项目归属校验。新增 agent-targets.test.ts 5 个测试（创建版本、拒绝缺配置、版本递增、test-invoke、越权 404），db-migrations 测试同步更新；13 个 PR 相关测试全部通过，后端 tsc 通过。

- [x] **任务 23**：PR-06 http\_agent 与统一 Target Adapter（Playground 接入）。
  - **完成时间**：2026-08-24

  - **结果说明**：新增 `services/target-adapter.ts`，作为 Playground / Experiment / Runner 统一调用入口。按 AgentTargetVersion.target\_type 分派：prompt\_model 走 invokePromptModelTarget；http\_agent 发送标准协议信封（protocolVersion 1.0、requestId、run、sample、context），附 X-AgentMonitor-Run-Id / Run-Item-Id / Request-Id 关联头，支持 bearer / api\_key / none 鉴权与自定义 headers，使用 AbortController 做超时控制；支持 requestTemplate 白名单变量渲染（{{sample.input}}、{{run.id}}、{{context.traceId}} 等，禁止任意 JS 表达式），通过 RFC 6901 JSON Pointer 抽取 outputPath / traceIdPath / usagePath；external\_runner 与 trace\_replay 显式拒绝内联调用。`TargetInvokeResult` 新增 `traceId` 字段，用于回填 Agent 侧上报的 traceId。重构 `services/playground.ts`：runPlaygroundModel 与 compareModels 统一走 invokeTarget，支持显式 targetVersionId，旧 modelConfigId/model 入参自动构造临时 prompt\_model TargetVersion（不入库）；保留 Playground"换模型不换 Prompt"的临时调试体验，metadata 写入 target\_type / target\_version\_id / target\_trace\_id。`POST /api/playground/run` 支持 `target_version_id` 入参。放宽 prompt\_model Target 必须带 promptId/promptVersionId 的限制（允许只选模型直接对话，callTargetModel 已支持）。新增 tests/target-adapter.test.ts 8 个测试（标准信封+关联头、bearer 鉴权、api\_key 鉴权、模板渲染、Agent status=error、HTTP 503、AbortController 超时、无 outputPath 回退），Playground 与 Agent Target 相关测试同步通过；全量后端回归 52 文件 580/581 通过（唯一失败为 evaluation-runner 在全量并发下的已知 DB 隔离偶发，单独运行 4/4 通过），tsc --noEmit 无错误。

- [x] **任务 24**：PR-07 DatasetVersionItem 与 Evaluator Suite。
  - **完成时间**：2026-08-24

  - **结果说明**：新增 p004\_dataset\_items\_and\_suites 迁移（SQLite + PostgreSQL 同编号），创建 dataset\_version\_items（id、dataset\_version\_id、dataset\_item\_id、case\_key、input\_data、expected\_data、context\_data、fields、metadata、tags、content\_hash、ordinal，UNIQUE(dataset\_version\_id, case\_key)）、evaluator\_suites、evaluator\_suite\_versions、evaluator\_suite\_members（UNIQUE(suite\_version\_id, alias)，weight、required、pass\_threshold、ordinal）四张表。迁移内置历史 DatasetVersion 回填逻辑：解析老 item\_data 数组，对每条样本生成稳定 caseKey（取 dataset\_item\_id 或 `case_N`），按输入/期望生成 SHA-256 content\_hash，按 ordinal 写入。SQLite `initSqliteSchema` 同步创建四张表。新增 `services/dataset-version-item.ts`：createDatasetVersionItems（重复 caseKey 立即报错）、listDatasetVersionItems（按 ordinal 稳定排序）、getDatasetVersionItem、countDatasetVersionItems、diffDatasetVersions（按 caseKey 分类 added/removed/changed/unchanged，供基线对比）、buildSnapshotFromWorkingCopy；字符串输入按 P0 兼容规则包装为 input `{query}`/expected `{answer}`。`commitDatasetVersion` 在提交 DatasetVersion 时同步写入逐样本快照。新增 `services/evaluator-suite.ts`：createEvaluatorSuite（自动建 v1）、createEvaluatorSuiteVersion（版本号递增、current\_version\_id 指向新版本、历史版本不可变）、list/get、listSuiteVersions/Members，成员校验 alias 非空/唯一、weight 非负、evaluatorVersionId 存在；aggregateScores 实现 weighted\_avg（按 weight 求加权平均并与 passThreshold 比较，带 1e-9 浮点容差）、all\_required（所有 required 成员必须通过）、any\_pass（任一成员通过即通过）三种聚合策略，返回 compositeScore、perAlias 明细和失败原因。新增路由 `routes/evaluator-suites.ts` 与 `routes/dataset-version-items.ts`，注册于 `/api/v2/evaluation`，全部走 authMiddleware 与项目归属校验；Suite 提供 CRUD 与版本成员接口，DatasetVersionItem 提供只读列表/单条/diff 接口（写入统一走 DatasetVersion 提交，禁止绕过快照直接改）。新增 tests/evaluator-suite.unit.test.ts 7 个纯聚合规则测试，tests/dataset-suite.test.ts 8 个端到端 API 测试（快照字段完整、caseKey 404、Suite 创建+成员+current\_version\_id、重复 alias 拒绝、版本递增+历史不可变、幽灵 evaluatorVersionId 拒绝、越权 404、v1→v2 diff 分类），db-migrations.test.ts 增加 p004 迁移注册、幂等升级、历史版本回填与 UNIQUE 约束验证。全量后端回归 54 文件 **597 测试全部通过**，tsc --noEmit 无错误。

- [x] **任务 25**：PR-08 Experiment/Run 分离。
  - **完成时间**：2026-08-25

  - **结果说明**：新增 p005\_experiment\_run\_separation 迁移（SQLite + PostgreSQL 同编号），为 evaluation\_experiments 追加 dataset\_version\_id、target\_version\_id、evaluator\_suite\_version\_id、lifecycle\_status、default\_run\_config、default\_gate\_config 六个字段（SQLite 通过 `PRAGMA table_info` 做幂等 ADD COLUMN，PostgreSQL 使用 `ADD COLUMN IF NOT EXISTS`），并创建 evaluation\_runs（UNIQUE(experiment\_id, run\_number)，含 status、config\_snapshot、lease\_owner/lease\_expires\_at/lease\_token\_hash、summary\_json、gate\_result、error、started\_at/completed\_at 等）、evaluation\_run\_items（UNIQUE(run\_id, dataset\_version\_item\_id)，含 status、input\_data、expected\_data、target\_output、target\_trace\_id、target\_error、latency\_ms、attempts、score\_json）、evaluation\_scores（UNIQUE(run\_item\_id, evaluator\_alias)，含 evaluator\_version\_id、score、passed、reason、raw、metrics\_json、started\_at/completed\_at）、evaluation\_run\_events（UNIQUE(run\_id, sequence)，append-only 事件流，含 event\_type、payload\_json、actor、created\_at）四张表及配套索引；SQLite `initSqliteSchema` 同步创建。新增 `services/evaluation-run.ts`（约 870 行）承载 Run/RunItem/Score/RunEvent 领域：`validateExperiment` 校验 Target/Dataset/EvaluatorSuite 版本就绪并收集阻断/警告问题；`prepareRun` 锁定三个版本快照、写入 Run（config\_snapshot）和按 DatasetVersionItem 顺序展开的 RunItem、追加 run\_queued 事件，返回 RunPreparation；`markRunRunning`/`completeRun`/`failRun`/`requestCancelRun` 管理生命周期；`recordTargetResult`/`markRunItemFinal` 回写目标结果；`upsertScore` 按 (run\_item\_id, evaluator\_alias) 幂等写入评测结果；`appendRunEvent` 以单调递增 sequence 写事件流；`getRunReport` 汇总 total/passed/failed/avgScore/avgLatency/scoreDistribution/gateResult；`getLatestRunByExperiment` 供报告兼容层读取；预留 `generateLeaseToken`/`hashLeaseToken`（SHA-256）与 lease 字段供 PR-09 Worker 抢占使用。新增 `services/evaluation-v2-runner.ts` 的 `executePreparedRun`：标记 running → 逐 RunItem 抽取 input（优先 input.query，其次最后一条 user message）调用 `invokeTarget` → 逐 Suite 成员加载 EvaluatorVersion 并调用 `evaluateOutput`（复用旧 Runner）→ `aggregateScores` 聚合 → 写 Score/RunItem/item\_completed|item\_failed 事件 → 汇总后 completeRun + run\_completed 事件，过程通过 onProgress 回调广播 WebSocket。新增 `routes/evaluation-runs.ts`（注册于 `/api/v2/evaluation`）：`POST /experiments/:id/validate`、`POST /experiments/:id/runs`（prepareRun 返回 201）、`GET /experiments/:id/runs`、`GET /runs/:id`、`POST /runs/:id/cancel`（不可取消状态返回 409）、`GET /runs/:id/items`、`GET /runs/:id/events`、`GET /run-items/:id/scores`，全部走 authMiddleware 并通过 JOIN project\_id 做归属校验。改造 `routes/evaluation.ts` 的 start/report 路由实现 V2/Legacy 双轨：`POST /experiments/:id/start` 根据是否存在 target\_version\_id 或 evaluator\_suite\_version\_id 判定 V2，V2 路径执行 validateExperiment → prepareRun → 立即返回 202 `{run_id, experiment_id, status:'queued'}`，随后 `setImmediate` 异步执行 executePreparedRun；Legacy 路径保持原 startExperiment+runExperiment 不变。`GET /experiments/:id/report` 同样分支：V2 读取 latest Run + getRunReport，返回旧版形态字段（totalItems/passedCount/failedCount/passRate/avgScore/avgLatency/scoreDistribution/items 等）并附加 run 对象，保证前端零改动兼容。`services/evaluation.ts` 扩展 EvaluationExperiment 接口六个新字段、`createExperiment` 接收 v2Options（三版本 ID + 默认 run/gate 配置）并在三者齐全时置 lifecycle\_status=ready、`getExperimentById` 解析 JSON 配置、新增 `getEvaluatorVersionById`。修复关键基础设施缺陷：`db/sqlite.ts` 的 `convertParams` 原先按 `$n` 编号去重传参，导致同一编号在 SQL 中出现多次时 better-sqlite3 报 "Too few parameter values"（upsertScore 中 `$5` 同时用于 completed\_at CASE 判断）；改为按每个 `$n` 出现顺序逐个 push 参数值（重复引用重复传值），彻底修复 V2 评分写入失败问题。限流改为显式开关：仅当 `NODE_ENV=production` 或 `RATE_LIMIT_ENABLED=true` 时注册 @fastify/rate-limit，测试默认关闭，rate-limit.test.ts 在 import app 前开启该变量；.env 同步将 RATE\_LIMIT\_MAX 提升至 100000 避免本地误触。新增 tests/evaluation-runs.test.ts 7 个端到端测试（validate 返回 valid、POST /runs 创建 queued Run+RunItem、POST /start 返回 202 且异步执行完成后 2 条 item 全部 succeeded 且 scores/events 完整、GET /report 兼容形态、runs 列表 run\_number 唯一递增、空 Experiment validate 返回 issues、cancel queued Run 成功且 completed Run 返回 409）；db-migrations.test.ts 增加 p005 注册与幂等验证；rate-limit.test.ts 适配新开关。全量后端回归 **55 文件 604 测试全部通过**，`tsc --noEmit` 无错误。

- [x] **任务 26**：PR-09 数据库 Worker（claim/lease/heartbeat/recovery、取消、重试）。
  - **完成时间**：2026-08-25

  - **结果说明**：在 PR-08 的 Run/RunItem/Score/RunEvent 表结构之上，落地持久化数据库 Worker，替代此前 start 路由内 `setImmediate(executePreparedRun)` 的进程内异步执行。`services/evaluation-run.ts` 新增一组队列原语：`claimRunItem(workerId, leaseSeconds, {runId?})` 以"先 SELECT 候选 id、再带条件 UPDATE"的短事务原子领取 status='pending' 项，写入 lease\_owner（hostname-pid-random）、lease\_token\_hash（SHA-256）、lease\_expires\_at（ISO 字符串），置 status='running'、phase='target'、attempt\_count+1，并在 UPDATE 中通过 `EXISTS (SELECT 1 FROM evaluation_runs WHERE status IN ('queued','running'))` 二次校验，避免 SELECT/UPDATE 间隙 Run 被取消；PostgreSQL 路径已预留 FOR UPDATE SKIP LOCKED 接口位置。`heartbeatRunItem` 仅在 lease\_token\_hash 匹配时续租，不匹配抛 `LeaseExpiredError`。`setRunItemPhase` 推进 target→evaluators 阶段。`requeueExpiredLeases(now)` 扫描过期项并按阶段感知回收：target 阶段或 target\_output 为空的项清理输出回到 pending/queued 重新调用 Target，evaluators 阶段且已有 target\_output 的项保留输出回到 pending/evaluators 仅补评分（避免崩溃后重复调用 Agent）；时间比较使用 `CAST(lease_expires_at AS TEXT) < CAST($1 AS TEXT)` 同时兼容 SQLite ISO 字符串与 PostgreSQL timestamptz。`recoverStaleRunsOnStartup` 在应用启动时把所有残留 running 项按阶段感知回收，并把仍为 running 但已无未完成项的孤儿 Run 置 failed（进程崩溃保护）。`cancelRunItem` 按可选 leaseToken 校验后把项置 cancelled；`finalizeCancellationIfReady` 在 cancelling Run 已无 pending/running 项时把所有非终态项兜底置 cancelled 并把 Run 置 cancelled，写 run\_cancelled 事件；`requestCancelRun` 放宽为支持 queued/running/cancelling 三种状态（幂等重复取消）。`failRunItem`/`clearLease`/`countIncompleteRunItems` 提供失败终态与租约释放。`aggregateAndCompleteRun` 基于当前所有 RunItem 的 Score 汇总 summary（passed/failed/skipped/avgScore/avgLatency/totalTokens）与 gateResult（failedItems===0），幂等完成 Run。`createRetryRun(sourceRunId)` 基于源 Run 中 status='failed' 的项创建新 Run，`retry_of_run_id` 指向源 Run，trigger\_type='retry'；通过 prepareRun + `pruneRunItemsToDatasetVersionItems` 裁剪，仅保留源失败项对应的 DatasetVersionItem。`appendRunEvent` 增加 MAX(sequence)+1 冲突重试（最多 5 次、5ms 退避），解决多 Worker 并发 claim 时 UNIQUE(run\_id, sequence) 冲突。新增 `services/evaluation-worker.ts`：`EvaluationWorker` 单例维护 WORKER\_ID、两个 unref() 定时器（500ms poll + 10s reap）和 Run 上下文缓存（target/suite/members 按 runId 缓存避免每条 item 重复查库）；`tick()` 每轮最多连续领取 8 条项，先执行 `finalizeCancellingRuns()` 把所有 cancelling Run 的 pending 项快速置 cancelled 并终态化，再循环 claim+`handleClaimed`；`handleClaimed` 加载上下文后调用 `processRunItem`，在每条 item 完成后根据 incomplete 数量决定 aggregateAndCompleteRun 或 finalizeCancellationIfReady；`reap()` 调用 requeueExpiredLeases；`start()` 先执行一次 recoverStaleRunsOnStartup 再安装定时器，`poke()` 供 start/retry/cancel 路由主动唤醒以缩短首个任务的 500ms 延迟，`waitForRun(runId, timeoutMs)` 供测试/CLI 同步等待终态。`services/evaluation-v2-runner.ts` 重构为 Worker 友好的 `processRunItem(claimed, ctx)`：进入前/Target 后/每个 Evaluator 前检查 isRunCancelling；Target 调用抽离为 `invokeTargetWithRetry`，对 AbortError/timeout/network/5xx 按指数退避（250ms→500ms→1s，上限 5s）重试 DEFAULT\_TARGET\_MAX\_RETRIES=2 次，非可重试错误直接失败；心跳定时器按租约 1/3 周期续租（最少 5s），在 finally 中 clearInterval 并 clearLease；阶段感知：item.target\_output 已存在则跳过 Target 直接补评分（崩溃恢复路径）。保留 `executePreparedRun` 作为内联回退/历史测试路径，同样使用 invokeTargetWithRetry。`routes/evaluation.ts` V2 start 路由改为 prepareRun 后立即返回 202，调用 `getEvaluationWorker().start()` + `poke()`，不再 setImmediate 内联执行。`routes/evaluation-runs.ts` 新增 `POST /runs/:id/retry`（源 Run 必须 completed/failed，无失败项返回 409），cancel/retry/POST runs 路由均 poke 唤醒 Worker。`app.ts` 在非 test 环境或 EVAL\_WORKER\_ENABLED=true 时启动 Worker；`index.ts` 在 app.listen 后 `startEvaluationWorker()`，SIGTERM/SIGINT 关停时 `stopEvaluationWorker()`。进度通过 WebSocket 广播 `evaluation.run.progress`（projectId/runId/current/total/completionRate/status/compositeScore），前端刷新后可通过 `GET /runs/:id/events` 回放完整事件流。新增 tests/evaluation-worker.test.ts 7 个测试（claim 原子领取+重复领取返回 null、heartbeat 正确 token 续租成功+错误 token 抛 LeaseExpiredError、requeueExpiredLeases target 阶段清理输出、evaluators 阶段保留 target\_output、recoverStaleRunsOnStartup 崩溃恢复、createRetryRun 仅复制失败项且 retry\_of\_run\_id 正确、Worker.tick 通过 claim 执行 Run 至 completed 且 score/event 完整）；tests/evaluation-runs.test.ts 扩展到 8 个测试（cancel 路径改为直接 service 层 prepareRun+requestCancelRun+Worker.tick 以避免与全局 Worker 竞态，新增 retry 接口 409 测试）。测试中显式 stopEvaluationWorker 隔离后台轮询。全量后端回归 **56 文件 612 测试全部通过**，`tsc --noEmit` 无错误。

- [x] **任务 27**：PR-10 External Runner、Service Token 与 CLI。
  - **完成时间**：2026-08-25

  - **结果说明**：新增 p006\_service\_tokens\_runner\_sessions 迁移（SQLite + PostgreSQL 同编号），创建 `service_tokens` 表（id/project\_id/name/token\_hash/token\_prefix/scopes JSON/expires\_at/last\_used\_at/revoked\_at/created\_by/时间戳，token\_hash 唯一，project\_id 索引）和 `runner_sessions` 表（id/project\_id/run\_id/token\_id/runner\_id/capabilities JSON/status/last\_heartbeat\_at/closed\_at/metadata/时间戳，run\_id 与 project\_status 索引）；SQLite `initSqliteSchema` 同步建表，db-migrations.test.ts 期望迁移数更新为 6。新增 `services/service-token.ts`：`generateServiceToken()` 生成 `amt_` 前缀 36 字符 token，`hashServiceToken()` SHA-256；`createServiceToken(projectId, name, {scopes, expiresAt, createdBy})` 返回一次明文；`listServiceTokens/getServiceTokenById/revokeServiceToken` 管理生命周期；`verifyServiceToken(plain)` 校验前缀/hash/未撤销/未过期，更新 last\_used\_at，返回 token 与 projectId；支持 8 个 scope（telemetry:write、prompts:read、datasets:read、targets:read、targets:invoke、evaluation:run、evaluation:read、evaluation:result:write）。重写 `middleware/auth.ts`：`authWithScopes(...requiredScopes)` 工厂函数同时支持用户 JWT 与 Service Token，按 `amt_` 前缀分流；Service Token 缺少 scope 返回 403 INSUFFICIENT\_SCOPE；把 projectId/authType 挂到 request；原 `authMiddleware` 保留为 `authWithScopes()` 无 scope 限制的快捷方式。新增 `routes/service-tokens.ts`（注册于 `/api/v2`）：`GET /service-tokens/scopes` 返回合法 scope 列表，`POST /service-tokens` 创建（仅项目所有者），`GET /service-tokens?projectId=` 列表（不返回 hash），`DELETE /service-tokens/:id` 撤销。新增 `services/runner-session.ts`：`createRunnerSession(projectId, runId, token, {runnerId, capabilities, metadata})` 创建会话（Run 必须在同一项目且非终态，同 Run 只允许一个 active session）；`closeRunnerSession/heartbeatRunnerSession/getRunnerSession` 管理会话；`runnerClaimItem(sessionId, token, leaseSeconds)` 通过带 `{runId}` 的 claimRunItem 原子领取 RunItem，写入 session\_id 到 run\_items，返回 leaseToken 与 sample；`runnerHeartbeatItem/runnerSubmitResult/runnerFailItem` 分别续租、上传 Target 结果（写入 target\_output 并把状态置回 pending/evaluators 让平台 Worker 补评分）、上报失败；`reapStaleRunnerSessions(timeoutSeconds=90)` 回收 90s 未心跳的会话。新增 `routes/runner.ts`（注册于 `/api/v2/evaluation`，全部需要 evaluation:run scope）：`POST /runner-sessions`、`POST /runner-sessions/:id/close`、`POST /runner-sessions/:id/heartbeat`、`POST /runner-sessions/:id/claim`、`POST /run-items/:id/heartbeat`、`POST /run-items/:id/target-result`（需要 evaluation:result:write 或 evaluation:run）、`POST /run-items/:id/fail`、`GET /runs-for-runner/:id`（供 Service Token 读取 Run 状态）。`services/evaluation-run.ts` 的 `claimRunItem` 在未传 runId 时通过 `NOT EXISTS (SELECT 1 FROM runner_sessions WHERE run_id = i.run_id AND status = 'active')` 跳过有 active Runner 会话的 Run，避免平台 Worker 与 External Runner 竞争；传 runId 时不受限制（Runner 自己领取）。`services/evaluation-worker.ts` 的 `reap()` 增加 `reapStaleRunnerSessions()` 调用，每 10s 回收超时会话。CLI 新增正式入口：`cli/src/index.ts`（commander）支持 init/doctor/eval run/wait/report/gate/runner 子命令，`--profile/--base-url/--token/--project-id` 全局选项；`cli/src/config.ts` 实现 YAML profile（\~/.agentmonitor/config.yaml，支持 AGENTMONITOR\_CONFIG 覆盖、AGENTMONITOR\_TOKEN 环境变量、tokenEnv 字段），含零依赖极简 YAML 解析/序列化；`cli/src/api-client.ts` 封装 fetch 客户端与 Run 类型；`cli/src/commands/eval-v2.ts` 实现 eval run（触发 + 可选 wait）、eval wait（1s→5s 退避轮询直到终态）、eval report（JSON 输出/写文件）、eval gate（minPassRate 门禁）与退出码（0 通过、1 Gate 未达、2 参数错误、3 Target 错误、4 平台错误、130 取消）；`cli/src/commands/runner.ts` 实现 `runner <runId> --command <cmd...>`：创建 Runner Session → spawn 子进程 → 按 NDJSON 协议（stdin `{type:'invoke',caseKey,input,expected}` / stdout `{type:'result',caseKey,output,error?,traceId?,latencyMs?,tokenUsage?}`）驱动本地 Agent，按 60s 单样本超时与 15s 会话心跳，循环 claim 直到 Run 完成。`cli/tsconfig.json` 新建 ES2022/Bundler 配置。新增 tests/external-runner.test.ts 3 个测试：scopes 列表返回、read-only scope token 被 403、完整 External Runner 流程（prepareRun → createSession → claim → target-result → close → Worker.tick 补评分 → Run completed 且 passedItems=1，Service Token 通过 runs-for-runner 读取结果）。db-migrations.test.ts 期望迁移数更新为 6。全量后端回归 **57 文件 615 测试全部通过**，后端 `tsc --noEmit` 无错误。

- [x] **任务 28**：PR-11 Run 报告、Bad Cases、Run 对比与 JUnit 导出。
  - **完成时间**：2026-08-25

  - **结果说明**：新增 `services/run-reports.ts` 作为 Run 结果分析层。`listRunItems(filter)` 支持按 status、passed、caseKey、caseKeyPrefix、evaluatorAlias、scoreMin/scoreMax、errorCode、cursor、limit 分页查询 RunItem，LEFT JOIN evaluation\_scores 一次性返回每条 item 的评测明细，游标分页使用 `i.id > cursor` + LIMIT N+1 稳定翻页；返回 camelCase 结构并在路由层转换为与历史 EvaluationRunItem 兼容的 snake\_case（target\_output/phase/scores 等），保证旧前端与测试零改动。`getBadCases(runId, limitPerEvaluator=20)` 按 evaluator\_alias 聚合 failed 评分项，同时收集 Target 调用失败的 item（target\_error 非空），输出按 alias 分组的失败样例与计数，供报告页快速定位回归。`compareRuns(baselineRunId, candidateRunId)` 基于两次 Run 的 caseKey 集合做集合运算：fixedCases（基线失败、候选通过）、regressedCases（基线通过、候选失败）、stillFailing（两次均失败）、newFailed（候选新增失败且基线不存在），每次对比校验两个 Run 同属一个 project，否则 404。`generateJunitXml(runId, run, items)` 生成标准 JUnit XML：testsuites 聚合 summary，每个 evaluator alias 一个 testsuite，每个 caseKey 一个 testcase，失败项输出 `<failure>` 元素并做 XML 转义，CI 系统（Jenkins/GitLab/GitHub Actions）可直接消费。`exportRun(runId)` 返回 {run, items, events} 的完整 JSON 导出。`routes/evaluation-runs.ts` 扩展：`GET /runs/:id/items` 接收全部过滤查询参数；新增 `GET /runs/:id/report`（返回 run + items + badCases）、`GET /runs/:id/bad-cases?limit=`、`GET /runs/:id/export?format=junit|json`（junit 返回 `application/junit+xml`，默认 JSON）、`POST /runs:compare`（body {baselineRunId, candidateRunId}，同项目校验）。所有新路由统一走 authMiddleware 与 projectIdForRun + userOwnsProject 项目归属校验。CLI `eval report` 增加 `--junit` 开关：携带 Bearer Token 直接请求 `/export?format=junit`，支持 `-o <file>` 写文件或输出到 stdout，供 CI 管道上传测试报告。新增 tests/run-reports.test.ts 6 个端到端测试：/report 返回 run+items+badCases、/items?status=succeeded 过滤、/bad-cases 返回结构、/export?format=junit 返回 XML 且包含 testsuites/testcase/failure 标签、/export 默认 JSON、/runs:compare 正确分类 fixed/regressed/stillFailing/newFailed。修复测试搭建过程中发现的 evaluator versions 响应结构（`{versions:[...]}`）、suite 成员 alias 必填、createExperiment v2Options 位置参数等问题。全量后端回归 **58 文件 621 测试全部通过**（`--no-file-parallelism` 串行模式；并行模式下因共享 SQLite 文件 + 全局 Worker 单例存在已知偶发竞态，单文件运行均通过），后端 `tsc --noEmit` 无错误；CLI 14 测试通过，`tsc --noEmit` 无错误。

- [x] **任务 29**：PR-12 Prompt Runtime 与 Deployment 指针。
  - **完成时间**：2026-08-25

  - **结果说明**：新增 p007\_prompt\_deployments 迁移（SQLite + PostgreSQL 同编号），创建 `prompt_deployments` 表（id/project\_id/prompt\_id/environment/prompt\_version\_id/deployed\_by/note/created\_at/updated\_at，UNIQUE(project\_id, prompt\_id, environment)，外键 RESTRICT 防止删除已发布版本）；SQLite `initSqliteSchema` 同步建表并加索引；db-migrations.test.ts 期望迁移数更新为 7。新增 `services/prompt-deployment.ts`：定义 production/staging/development 三种合法环境，`deployPromptVersion` 通过 UPSERT 原子移动 `(project,prompt,environment)` 三元组指向的 PromptVersion 指针，不覆盖版本内容；`listDeployments/getDeployment/deleteDeployment` 提供管理能力；`resolveDeployedPrompt(projectId, promptName, environment)` 按项目内 Prompt 名称 + 环境解析当前发布的不可变 PromptVersion；`buildRuntimeResponse` 装配 content、variablesSchema、modelDefaults、versionNumber、deployedAt 与稳定 ETag（基于 deployment+version+content 的 SHA-256 前 24 位），供 SDK 做 If-None-Match/304 缓存。新增 `routes/prompt-deployments.ts` 注册于 `/api/v2`：`GET /prompt-deployments/environments`、`GET /prompt-deployments?projectId=&promptId=`、`GET /prompt-deployments/:promptId?environment=`、`PUT /prompt-deployments/:promptId`（body: projectId/promptVersionId/environment/note）、`DELETE /prompt-deployments/:promptId?projectId=&environment=`，所有写操作走 authMiddleware + projects.user\_id 归属校验；`GET /runtime/prompts/:promptName?environment=production` 作为 Runtime 解析入口，JWT 用户通过 `X-Project-Id` 头或 `?projectId=` 指定项目，Service Token 自动绑定所属项目，返回 `Cache-Control: private, max-age=60` 与 ETag，If-None-Match 命中直接 304。新增 tests/prompt-deployments.test.ts 6 个端到端测试：环境列表、首发 v1+GET 读取、Runtime 返回 content/variablesSchema/modelDefaults/ETag 并命中 304、移动指针到 v2 后 Runtime 立即返回新 ETag 与 versionNumber=2、未发布环境 404+非法 environment 400、DELETE 下线后 404。修复路由中误用 owner\_id（实际列为 user\_id）导致的 500。全量后端回归 **59 文件 627 测试全部通过**，`tsc --noEmit` 无错误。

- [x] **任务 30**：PR-11 前端 Run Detail / Bad Cases / Compare / Export。
  - **完成时间**：2026-08-26

  - **结果说明**：在 `frontend/src/api.ts` 新增 `runsV2` 客户端与 V2 类型（V2Run/V2RunItem/V2Score/V2RunEvent/V2RunItemsPage/V2BadCasesReport/V2RunComparison/V2RunReport 等），对接后端 `/api/v2/evaluation/runs/:id` 系列接口：`listByExperiment/get/report/items/events/badCases/compare/exportJunit/exportJson`，其中 items 支持 status/passed/caseKey/evaluatorAlias/score 区间/errorCode/cursor/limit 过滤，exportJunit 通过 fetch 携带 Bearer Token 拉取 XML 文本并触发浏览器下载。新增 `frontend/src/pages/RunDetailPage.tsx`（路由 `/evaluation/runs/:id`）：顶部展示 Run 编号、状态徽章、触发方式、开始时间与"刷新/对比/导出 JSON/导出 JUnit"操作；5 张汇总卡片（总数/通过/失败/平均得分/平均延迟）；Bad Cases 区块按 targetErrors 与 byEvaluator 分组展示，包含 caseKey、score、error、targetError；Run Items 表格支持按状态/通过情况/evaluator alias 三维过滤，行内显示每个 evaluator 的得分与 PASS/FAIL 徽章、target\_output 摘要与错误、latency、Trace 跳转链接（`/traces/:traceId`）；事件流区块按 sequence 展示 run\_event 类型与 payload JSON。新增 `frontend/src/pages/RunComparePage.tsx`（路由 `/evaluation/runs/compare`）：输入 experimentId 加载该实验下所有 Run 并按 run\_number 倒序，默认选中最近两次作为基线/候选；展示双方通过率与差值（正负绿红色），四象限分类卡片分别渲染 fixedCases/regressedCases/stillFailing/newFailed；URL 通过 `?experimentId=&baseline=&candidate=` 持久化选择，支持从列表页直达。`frontend/src/App.tsx` 注册两个新路由（compare 放在 :id 之前以避免被参数化匹配）。`frontend/src/pages/EvaluationPage.tsx` Experiments Tab 操作列新增紫色"Run 对比"按钮，跳转 `/evaluation/runs/compare?experimentId=<id>`。前端 `npx tsc --noEmit` 无错误，`npm run build` 成功产出 `RunDetailPage-*.js`（10.47 kB）、`RunComparePage-*.js`（5.31 kB）两个 lazy chunk。

- [x] **任务 31**：PR-12 前端 Prompt Deployment 管理与 Runtime 调试。
  - **完成时间**：2026-08-26

  - **结果说明**：`frontend/src/api.ts` 新增 `PromptDeployment` 与 `ResolvedRuntimePrompt` 类型，新增 `promptDeployments` 客户端，封装 `environments / list / get / deploy / remove / resolve` 六个方法，其中 `resolve` 直接调用 `/api/v2/runtime/prompts/:name` 并处理 304 Not Modified 与 ETag 回传，可用于后续 SDK 的缓存逻辑。`frontend/src/pages/PromptsPage.tsx` 在原有 prompts/versions/runs/configs 四个 Tab 之外新增"部署"Tab（`PROMPT_ENVIRONMENTS = production / staging / development`），需要先在左侧选中一个 Prompt 才能操作，三栏卡片分别展示每个环境的当前指针：已发布时显示 version\_number、版本 ID 前 8 位、发布备注、updated\_at 与"下线"按钮；未发布时显示灰色占位。发布表单由"目标环境 + Prompt Version 下拉（按 version\_number + description 渲染）+ 备注 + 发布按钮"组成，调用 `PUT /api/v2/prompt-deployments/:promptId` 后自动刷新部署列表与 Prompt 主体。Runtime 调试面板提供环境切换与"GET /runtime/prompts/:name"按钮，调用 resolve 后展示 version、ETag、content，并提供可折叠的 curl 示例（含真实 projectId、完整 origin、当前环境），方便复制到终端验证；移除了原来对 selectedPrompt 的重复 useEffect，合并为统一 effect 同时拉取 versions 与 deployments。前端 `npx tsc --noEmit` 无错误，`npm run build` 成功，PromptsPage chunk 由 22.60 kB 增长到 28.92 kB（gzip 6.22 kB），与新增部署面板体量一致。

- [x] **任务 32**：PR-12 三语言 SDK Runtime 客户端与 ETag 缓存。
  - **完成时间**：2026-08-26

  - **结果说明**：TypeScript SDK（`sdk/`）新增 `src/prompt-runtime.ts`，实现 `PromptRuntimeClient`，对应后端 `GET /api/v2/runtime/prompts/:name?environment=`；内置 ETag/304 协商缓存（命中 304 时不反序列化 body，只延长 TTL），硬 TTL 默认 60 秒，缓存键 `projectId::environment::promptName`，支持 `forceRefresh`、自定义 `maxAgeMs`、AbortSignal 与注入 fetch（便于测试）。`AgentMonitor` 类新增 `getPrompt(name, options)` 与 `clearPromptCache()` 方法，`SDKConfig` 增加 `projectId`（显式指定，未传时按 `<projectId>_<rest>` 从 apiKey 解析）与 `promptCacheTtlMs` 字段；修复 `this.config` 的 `Required<>` 类型以容纳可选字段。`LLMRequest` 新增 `promptRef?: {id, versionId, name?, versionNumber?, environment?}`，`traceLLM` 检测到 promptRef 时自动把 `prompt.id / prompt.version_id / prompt.name / prompt.version_number / prompt.environment` 写入 `metadata`，Trace 后端可据此按 Prompt 版本聚合。新增 `src/prompt-runtime.test.ts` 7 个测试：首次请求、TTL 命中、304 续期、200 覆盖、环境隔离、非 2xx 抛错、clearCache。Python SDK（`sdk-python/`）新增 `agentmonitor/prompt_runtime.py`，同样实现 ETag/304 + TTL 双层缓存，使用 `threading.Lock` 保护内存缓存，使用 `requests.Session` 复用连接；`SDKConfig` 新增 `project_id` 与 `prompt_cache_ttl_sec` 字段（默认 60.0），`AgentMonitor.get_prompt(...)` / `clear_prompt_cache()` 公开；`trace_llm` 同步支持从 `request["prompt_ref"]` 写入 metadata（含 usage）。新增 `tests/test_prompt_runtime.py` 8 个测试，含一个 20 线程并发测试验证 TTL 内只有一次网络请求。Go SDK（`sdk-go/`）新增 `agentmonitor/prompt_runtime.go`，实现 `PromptRuntimeClient`（`sync.RWMutex` 保护 map 缓存），公开 `GetPromptOptions{Environment, MaxAge, ForceRefresh}`、`ResolvedPrompt`、`PromptEnvProduction/Staging/Development` 常量与 `Get / ClearCache / CacheSize` 方法；`SDKConfig` 新增 `ProjectID` 与 `PromptCacheTTL` 字段；新增 `PromptRef` 与 `LLMRequest` 类型，`TraceLLM` 在 `request` 为 `*LLMRequest` 且 `PromptRef != nil` 时把 prompt 元数据写入 metadata；`AgentMonitor.GetPrompt` 通过 `sync.Once` 懒加载客户端。新增 `agentmonitor/prompt_runtime_test.go` 7 个测试（基于 httptest），覆盖首次拉取、TTL 命中、If-None-Match 304 续期、forceRefresh 覆盖、环境隔离、404 报错、清空缓存，修复了与包内已有 `contains` 辅助函数重名的问题（改用 `strings.Contains`）。三语言全量回归：TypeScript SDK 42 测试全部通过（3 个文件），Python SDK 37 测试全部通过（含原有 29 + 新增 8），Go SDK `go vet ./...` 无错误、`go test -count=1 ./...` 全部通过。

- [x] **任务 33**：PR-13a Scheduled Run 定时回归（调度表 + Worker 到期触发 + V2 路由 + 前端）。
  - **完成时间**：2026-08-27

  - **结果说明**：新增 p008\_scheduled\_runs 迁移（SQLite + PostgreSQL 同编号），创建 `scheduled_runs` 表（id/project\_id/experiment\_id（外键 evaluation\_experiments）/name/schedule\_type(interval|cron)/interval\_minutes/cron\_expr/enabled/last\_run\_at/next\_run\_at/last\_run\_id（外键 evaluation\_runs，ON DELETE SET NULL）/last\_status/created\_by/时间戳），索引 `(enabled, next_run_at)` 支撑到期扫描、project\_id 与 experiment\_id 支撑列表查询；SQLite `initSqliteSchema` 同步建表，db-migrations.test.ts 期望迁移数更新为 8。新增 `services/scheduled-run.ts`：零依赖实现 5 段 cron 解析器（支持 `*`、数字、逗号列表、`a-b` 区间、`*/step` 与 `a-b/step` 步长，分钟/小时/日/月/周字段范围校验），`nextCronDate` 从基准时间按分钟向后扫描（一年内窗口）计算下次触发；`computeNextRun` 对 interval 类型按 last\_run\_at + interval\_minutes 滚动（含最小 1 分钟下限保护），cron 类型按表达式计算；`createScheduledRun` 创建时校验 Experiment 归属并预计算 next\_run\_at；`updateScheduledRun` 在调度规则或启用状态变化时重算 next\_run\_at（禁用清空）；`claimDueScheduledRun` 通过 `SELECT ... WHERE enabled=1 AND next_run_at <= now ORDER BY next_run_at LIMIT 1` + 条件 `UPDATE ... WHERE next_run_at = <旧值>` 原子领取，多 Worker 不会重复触发，领取同时把 next\_run\_at 滚动到下一次、last\_run\_at 置当前、last\_status 置 running；`triggerScheduledRun` 调用 `prepareRun(experimentId, {triggerType:'scheduled', triggerRef:scheduleId})` 生成 V2 Run 并回写 last\_run\_id/last\_status（created/failed），失败不影响调度滚动。`services/evaluation-worker.ts` 新增 30s 周期的 scheduleTimer（unref，不阻止进程退出），`runDueSchedules()` 每轮最多连续触发 10 条到期调度，触发后立即 `poke()` 唤醒 RunItem 处理；scheduleRunning 标志防止重入。新增 `routes/scheduled-runs.ts`（注册于 `/api/v2/evaluation`）：`GET /schedules?projectId=&experimentId=`、`POST /schedules`、`GET /schedules/:id?projectId=`、`PATCH /schedules/:id`（支持 name/scheduleType/intervalMinutes/cronExpr/enabled 局部更新）、`DELETE /schedules/:id?projectId=`、`POST /schedules/:id/run-now?projectId=`（立即触发返回 runId），全部走 authMiddleware + 项目归属校验（非所有者 404），序列化层把 SQLite 的 0/1 enabled 转布尔。前端 `api.ts` 新增 `ScheduledRun` 类型与 `schedules` 客户端（list/create/update/remove/runNow）；`EvaluationPage.tsx` 新增"定时调度"Tab：调度列表展示名称/所属实验/调度规则（间隔或 cron 代码块）/下次运行时间/上次状态（含 Run 跳转链接）/启用徽章，操作列提供"立即运行/停用-启用/删除"；新建弹窗支持名称、Experiment 下拉、固定间隔（分钟）与 Cron 表达式两种模式切换（附常用示例）。新增 tests/scheduled-run-cron.unit.test.ts 9 个纯单元测试（cron 解析、步长、非法表达式拒绝、每天/工作日/每月 1 号的 nextCronDate、interval 与 cron 的 computeNextRun）与 tests/scheduled-runs.test.ts 8 个端到端测试（interval 创建预计算 next\_run\_at、cron 创建与非法表达式 400、按 experimentId 过滤、PATCH 禁用清空 next\_run\_at 再启用重算、run-now 立即触发并回写 last\_run\_id、Worker 到期扫描自动触发 Run 且 next\_run\_at 滚动到未来、二次扫描无到期项返回 0、DELETE 后 404、非项目所有者 404）。全量后端回归 **61 文件 644 测试全部通过**（`--no-file-parallelism`，较任务 29 的 627 新增 17 个），后端 `tsc --noEmit` 无错误；前端 `tsc --noEmit` 无错误，`npm run build` 成功，EvaluationPage chunk 42.52 kB（gzip 7.77 kB）。

- [x] **任务 34**：PR-13b Trace Sampling 线上 Trace 采样回流（采样规则 + Worker 周期扫描 + V2 路由 + 前端）。
  - **完成时间**：2026-08-27

  - **结果说明**：新增 p009\_trace\_sampling\_rules 迁移（SQLite + PostgreSQL 同编号），创建 `sampling_rules` 表（id/project\_id/name/target\_dataset\_id（外键 datasets ON DELETE CASCADE）/trace\_type\_filter/name\_contains/status\_filter/error\_only/sample\_rate/max\_items\_total/enabled/matched\_count/sampled\_count/last\_scanned\_at/created\_by/时间戳）与 `sampled_traces` 台账表（id/rule\_id（外键 ON DELETE CASCADE）/trace\_id/dataset\_item\_id/sampled\_at，UNIQUE(rule\_id, trace\_id) 保证同一规则对同一 Trace 只回流一次），索引 (project\_id, enabled) 与 rule\_id；SQLite `initSqliteSchema` 同步建表，db-migrations.test.ts 期望迁移数更新为 9。新增 `services/trace-sampling.ts`：`createSamplingRule/updateSamplingRule/list/get/delete` 全套 CRUD，创建/更新时校验 sampleRate ∈ (0,1] 与目标 Dataset 归属；采样率采用**确定性 hash**（sha256(ruleId:traceId) 前 4 字节 / 2^32 < rate），同一 (规则, Trace) 决策稳定、多规则相互独立，不依赖随机数；`runSamplingScan(rule)` 每轮通过 `traces LEFT JOIN sampled_traces ... WHERE st.id IS NULL` 拉取未处理候选（最多 500 条），按 trace\_type/name\_contains/status/error\_only 过滤、按采样率命中，未命中也登记台账避免重复评估，命中项通过 `addDatasetItems` 回流到目标 Dataset（metadata 标记 source=trace\_sampling + traceId/traceType/traceName），达到 max\_items\_total 上限后继续统计 matched 但不再回流，结束后原子更新 matched\_count/sampled\_count/last\_scanned\_at；台账写入采用"先查后插"跨库幂等（避免 SQLite 专有 INSERT OR IGNORE 语法）；`runAllSamplingScans()` 遍历全部启用规则。`services/evaluation-worker.ts` 新增 60s 周期 samplingTimer（unref），`runSamplingScans()` 带重入锁汇总本轮回流总数。新增 `routes/trace-sampling-rules.ts`（注册于 `/api/v2/evaluation`）：`GET /sampling-rules?projectId=`、`POST /sampling-rules`、`GET/PATCH/DELETE /sampling-rules/:id`、`POST /sampling-rules/:id/run-now`（立即执行一轮返回 matched/sampled/skippedRate 统计），全部走 authMiddleware + 项目归属校验，序列化层把 0/1 布尔转真实布尔。前端 `api.ts` 新增 `SamplingRule` 类型与 `samplingRules` 客户端（list/create/update/remove/runNow）；`EvaluationPage.tsx` 新增"采样回流"Tab：规则列表展示名称/目标数据集/过滤条件徽章（type、名称关键词、status、仅错误）/采样率与上限/已回流与匹配计数/启用状态，操作列提供"立即采样/停用-启用/删除"；创建弹窗支持名称、目标 Dataset 下拉、Trace 类型、名称关键词、采样率、总量上限与"仅错误 Trace"复选框（用于构建 Bad Case 集）。新增 tests/trace-sampling.test.ts 6 个端到端测试（创建+非法 sampleRate 400+不存在数据集 4xx、类型/名称/错误过滤命中 3 条并回流且第二轮台账去重为 0、errorOnly 只回流错误样本、maxItemsTotal=2 时 matched≥3 但 sampled=2、PATCH 禁用后 run-now 不回流+DELETE 后 404、非所有者 404）与 tests/trace-sampling.unit.test.ts 4 个单元测试（rate=1 全中/极小率全不中、rate=0.5 时 1000 条命中率落在 45%\~55%、决策稳定性、不同规则决策独立性）。全量后端回归 **63 文件 654 测试**（新增 10 个；`--no-file-parallelism` 下偶发 playground/decisions 网络类用例 ECONNRESET，单文件重跑均 100% 通过，与本次改动无关），后端 `tsc --noEmit` 无错误；前端 `tsc --noEmit` 无错误，`npm run build` 成功，EvaluationPage chunk 50.82 kB（gzip 8.95 kB）。

- [x] **任务 35**：PR-13c 持久化 Alert Rule / Webhook（规则表 + 事件台账 + Run 终态自动评估 + V2 路由 + 前端）。
  - **完成时间**：2026-08-27

  - **结果说明**：新增 p010\_persistent\_alert\_rules 迁移（SQLite + PostgreSQL 同编号），创建 `alert_rules` 表（id/project\_id/name/event\_type(run\_failed|run\_regression|run\_completed)/condition/threshold/webhook\_url/channels JSON/enabled/cooldown\_minutes/last\_triggered\_at/created\_by/时间戳）与 `alert_events` 台账表（id/rule\_id（ON DELETE SET NULL，规则删除事件保留）/project\_id/event\_type/severity(info|warning|critical)/title/message/payload JSON/fingerprint/delivery\_status(pending|delivered|failed|skipped)/delivered\_at/delivery\_error/created\_at），UNIQUE(rule\_id, fingerprint) 保证同一 Run 同一规则只告警一次，索引 (project\_id, enabled, event\_type) 与事件 (project\_id, created\_at)；V1 内存告警服务（services/alert.ts）原样保留向后兼容，新体系为独立 V2 持久化层。SQLite `initSqliteSchema` 同步建表，db-migrations.test.ts 期望迁移数更新为 10。新增 `services/alert-rule.ts`：规则 CRUD（创建时 run\_regression 必须带 threshold，channels 默认 \["web"]，反序列化 JSON channels 与布尔 enabled）；`evaluateRunAlerts(projectId, ctx)` 在 Run 终态时被调用，按 event\_type 评估（run\_failed=状态 failed 触发 critical；run\_regression=completed 且 gate passRate < threshold 触发 warning；run\_completed=完成即 info），双层去重（fingerprint=sha256(ruleId:runId:status) 唯一约束 + cooldown\_minutes 冷却窗口），命中后若配置 webhook\_url 则 POST JSON 负载（event/severity/title/message/runId/runNumber/passRate/fingerprint，5s AbortController 超时，非 2xx 判失败），投递结果 delivered/failed(含 error)/skipped(无 webhook) 写入事件台账并回写 last\_triggered\_at；支持 fetchImpl 注入便于测试。`services/evaluation-worker.ts` 在 Run 聚合完成与失败兜底两个终态点调用新增私有方法 `evaluateAlertsForRun(runId)`（读取 gate\_result.passRate 的 0\~1 量纲与实验名，异常只记日志不影响主流程）。新增 `routes/alert-rules.ts`（注册于 `/api/v2/evaluation`）：`GET/POST /alert-rules`、`GET/PATCH/DELETE /alert-rules/:id`、`GET /alert-events?projectId=&limit=`，全部走 authMiddleware + 项目归属校验。前端 `api.ts` 新增 `AlertRuleV2/AlertEventV2` 类型与 `alertRulesV2` 客户端（list/create/update/remove/events）；`EvaluationPage.tsx` 新增"告警规则"Tab：规则表展示名称/事件类型彩色徽章/通过率阈值与 Webhook 配置状态/冷却时间/上次触发/启停状态，操作列提供停用-启用/删除；下方"最近告警事件"表展示时间、severity 徽章、消息（含 Run 跳转链接）与投递状态（已投递/失败原因/站内）；创建弹窗支持名称、三种事件类型下拉（回归类型动态显示通过率阈值）、Webhook 地址（附飞书机器人示例占位）与冷却分钟数。新增 tests/alert-rules.test.ts 6 个测试：创建规则 + run\_regression 缺 threshold 返回 400、列表/PATCH 停用/DELETE 后 404、evaluateRunAlerts 低通过率触发 Webhook（注入 fetch 断言 URL/body/runId，delivered 状态）+ 同 Run 指纹去重不重复投递 + 高通过率不触发、Webhook ECONNREFUSED 时 delivery\_status=failed 且事件仍落库（critical）、端到端启动 Run 后 Worker 自动评估生成 run\_completed 事件（vi.waitFor 轮询 alert-events 按 payload.runId 过滤）、非项目所有者 404。全量后端回归 **64 文件 660 测试全部通过**，后端 `tsc --noEmit` 无错误；前端 `tsc --noEmit` 无错误，`npm run build` 成功，EvaluationPage chunk 59.82 kB（gzip 10.31 kB）。至此 **PR-13（Scheduled Run / Trace Sampling / 持久化 Alert Rule）三项全部交付完成**。

- [x] **任务 36**：P1 增强（trace\_replay 回放评分 / Trace 人工标注 / Prompt A/B 流量分桶）+ 工程化（生产部署文档 / E2E 冒烟）。
  - **完成时间**：2026-08-27

  - **结果说明**：

    - **trace\_replay 回放评分**：`services/evaluation-v2-runner.ts` 的 target 阶段新增 `trace_replay` 分支——不重新执行 Agent，直接把样本快照中的历史 Trace 输出（expected\_snapshot）作为 target\_output 进入既有评分流水线（latency 记 0、不调用任何模型），缺失历史输出时明确报错；`trace_replay` Target 类型本就在 SUPPORTED\_TARGET\_TYPES 中且无需 modelConfig，配合"采样回流 Dataset"即可实现线上 Trace 抽样回放评测闭环。

    - **Trace 人工标注**（p011 迁移，SQLite+PG）：新增 `trace_annotations` 表（project\_id/trace\_id/run\_item\_id/root\_cause(prompt|model|tool|retrieval|data|evaluator|unknown)/verdict(good|bad|uncertain)/note/tags JSON，UNIQUE(project\_id, trace\_id)，按根因建索引）。新增 `services/trace-annotation.ts`：UPSERT 标注（枚举校验）、按根因/结论过滤列表、根因分布统计 `getRootCauseStats`、删除；新增 `routes/trace-annotations.ts`（注册于 `/api/v2`）：`GET /trace-annotations/enums`、`GET /trace-annotations?rootCause=&verdict=`、`GET/PUT/DELETE /trace-annotations/:traceId`、`GET /trace-annotations-stats`，PUT 前校验 Trace 存在且归属项目。前端新增 `components/trace/TraceAnnotationPanel.tsx`（结论三态按钮、根因下拉中文标签、备注、标签、保存/删除），挂载在 TraceDetailPage 详情页顶部。

    - **Prompt A/B 流量分桶**（p012 迁移，SQLite+PG）：新增 `prompt_ab_variants` 表（project/prompt/environment/variant\_key/prompt\_version\_id/weight(0-100)/note/enabled，UNIQUE(project,prompt,environment,variant\_key)）。新增 `services/prompt-ab-test.ts`：变体 CRUD（校验版本归属与权重范围）、`computeBucket` 基于 `sha256(promptId:env:bucketKey) % 100` 的确定性分桶（同一用户/会话稳定命中）、`pickVariantByBucket` 按权重游标命中、`selectAbVariant` 组合查询。`resolveDeployedPrompt` 增加 bucketKey 参数，命中变体时切换到变体版本并返回 variantKey/bucket；Runtime 路由 `GET /runtime/prompts/:name` 支持 `?bucketKey=` 或 `X-Bucket-Key` 头，响应体新增 variantKey/bucket 字段；新增 `/prompt-ab-variants/:promptId` GET、`/:promptId/:variantKey` PUT/DELETE 管理接口。前端 PromptsPage 部署 Tab 新增"A/B 实验分流"面板（环境切换、变体表 Key/版本/权重/删除、新增表单），Runtime 测试面板新增 bucketKey 输入与"命中变体 B（桶 N）/基线版本"徽章。

    - **工程化**：新增 `docs/生产部署指南.md`（架构组件、环境变量清单、Docker Compose 与 systemd 两种部署、Nginx/HTTPS/WS 反代、Service Token scope、自动能力运维检查表、Webhook 告警排障、pg\_dump 备份恢复、健康检查与常见问题、测试发布验证）。新增 `e2e/pr13-p1-features.spec.ts` Playwright 冒烟（采样规则/告警规则创建与非法采样率 400、采样 run-now、标注枚举与 404 契约、Prompt 发布+A/B 变体 100% 权重 Runtime 命中 variantKey=B、评测中心三个新 Tab UI 可见性）。db-migrations.test.ts 期望迁移数更新为 12。

    - 全量后端回归 **65 文件 664 测试全部通过**（新增 `tests/p1-trace-replay-annotation-ab.test.ts` 4 个端到端测试：trace\_replay Run 回放 completed 且 passedItems=1 不调模型、标注 PUT/GET/列表/统计/非法枚举 400/删除全流程、A/B 分桶确定性与权重边界、Runtime 按 bucketKey 分流到变体并可权重归零回落基线），后端 `tsc --noEmit` 无错误；前端 `tsc --noEmit` 无错误，`npm run build` 成功（PromptsPage 33.54 kB）。

### 【已完成】DoD-8 Bad Case 加入回归集与单条重跑收口

- [x] **任务 37**：补齐 Run Detail Bad Case 详情字段、加入回归集与单条重跑闭环。
  - **完成时间**：2026-08-29

  - **结果说明**：后端 `services/run-reports.ts` 扩展 RunItem/BadCases 返回结构，新增 `dataset_version_item_id`、`input_snapshot`、`expected_snapshot`、评分 `reasoning` / `raw_output` 等字段，满足 Bad Case 展示 input、expected、rawOutput、评分理由与 Trace 的需求；`services/evaluation-run.ts` 新增 `createSingleItemRetryRun(sourceRunId, sourceRunItemId)`，复用既有 `prepareRun + pruneRunItemsToDatasetVersionItems` 实现单条样本重跑；`routes/evaluation-runs.ts` 新增 `POST /api/v2/evaluation/run-items/:id/retry` 与 `POST /api/v2/evaluation/run-items/:id/add-to-dataset`，支持把单条 Bad Case 沉淀到现有或新建数据集，并在 metadata 中保留 run/runItem/trace/score 上下文。前端 `api.ts` 新增对应客户端方法与类型，`RunDetailPage.tsx` 新增版本快照区、回归集目标选择区、Bad Cases 详情卡片、Run Items 可展开详情，以及“加入回归集 / 单条重跑”按钮，页面可直接完成 DoD-8 操作闭环。新增后端测试覆盖新字段、新接口与单条重跑/加入数据集流程；验证通过：`backend` 定向测试 `tests/evaluation-runs.test.ts`、`tests/run-reports.test.ts` 全部通过，后端 `npx tsc --noEmit` 通过，前端 `npx tsc --noEmit` 通过，`frontend npm run build` 通过（`RunDetailPage` 构建产物 20.92 kB，gzip 4.61 kB）。

### 【已完成】DoD-3 前端 Target 表单支持 http\_agent / trace\_replay

- [x] **任务 38**：升级快速评测向导第三步，支持创建 `prompt_model` / `http_agent` / `trace_replay` Target，并以 V2 版本链路创建实验。
  - **完成时间**：2026-08-29

  - **结果说明**：后端 `routes/evaluation.ts` 的 `POST /api/evaluation/experiments` 已支持接收 `dataset_version_id`、`target_version_id`、`evaluator_suite_version_id`、`default_run_config`、`default_gate_config` 并透传到 `createExperiment(..., v2Options)`，可直接创建 `lifecycle_status=ready` 的 V2 Experiment。前端 `api.ts` 新增 DatasetVersion、EvaluatorVersion、AgentTarget、EvaluatorSuite 等类型，以及 `evaluation.datasets.versions`、`evaluation.evaluators.versions`、`targetsV2`、`suitesV2` 客户端；`EvalWizard.tsx`、`EvalWizardStep2.tsx`、`EvalWizardStep3.tsx` 升级为真正的 V2 向导链路：第二步自动解析评测器当前版本，第三步新增 Target 类型选择和表单，`prompt_model` 支持 Prompt/模型配置/temperature/maxTokens，`http_agent` 支持 URL、method、headers、timeout、requestTemplate、output/trace/usage 路径与 bearer/api\_key 鉴权，`trace_replay` 提供回放说明并创建空配置 Target。提交时按顺序自动创建 DatasetVersion、TargetVersion、EvaluatorSuiteVersion，再创建并启动绑定版本的 Experiment。新增后端测试 `tests/evaluation.test.ts` 覆盖 V2 version 字段创建实验；验证通过：`backend npm test -- --no-file-parallelism tests/evaluation.test.ts` 通过，后端 `npx tsc --noEmit` 通过，前端 `npx tsc --noEmit` 通过，`frontend npm run build` 通过（`EvaluationPage` 构建产物 59.82 kB，gzip 10.31 kB）。

### 【已完成】DoD-11 Scheduled Run 执行历史

- [x] **任务 39**：为 Scheduled Run 增加执行历史落库、查询接口与前端展示。
  - **完成时间**：2026-08-29

  - **结果说明**：后端新增 `p013_scheduled_run_executions` 迁移，在 `migration-registry.ts` 和 `sqlite.ts` 中补充 `scheduled_run_executions` 表，记录 `schedule_id/project_id/experiment_id/trigger_mode/status/run_id/error_message/started_at/completed_at/next_run_at`，同时新增索引支持按调度和项目倒序查询；`services/scheduled-run.ts` 新增 `ScheduledRunExecution` 模型以及 `listScheduledRunExecutions()`、`startScheduledRunExecution()`、`completeScheduledRunExecution()`，并将自动到期触发与“立即运行”统一接入执行历史落库；`routes/scheduled-runs.ts` 新增 `GET /api/v2/evaluation/schedules/:scheduleId/executions?projectId=&limit=` 接口，同时让 `run-now` 以 `manual` 模式记录历史。前端 `api.ts` 新增 `ScheduledRunExecution` 类型与 `api.schedules.executions(...)` 客户端；`EvaluationPage.tsx` 的“定时回归调度”Tab 新增“执行历史”展开按钮，可展示最近 10 次执行的开始时间、触发方式、状态、Run 链接、完成时间、下次运行和错误信息。验证通过：`backend tests/db-migrations.test.ts` 与 `tests/scheduled-runs.test.ts` 通过，后端 `npx tsc --noEmit` 通过，前端 `npx tsc --noEmit` 通过，`frontend npm run build` 通过（`EvaluationPage` 构建产物 62.39 kB，gzip 10.80 kB）。

### 【已完成】验收收口与 E2E 冒烟修复

- [x] **任务 40**：完成本轮 DoD 收口项的全量回归、前端构建与关键 E2E 验收，并修复本地冒烟脚本契约漂移问题。
  - **完成时间**：2026-08-29

  - **结果说明**：后端执行完整回归 `npm test -- --no-file-parallelism`，最终 **65 个测试文件、666 条测试全部通过**；前端执行 `npx tsc --noEmit` 与 `npm run build` 均通过，最新构建产物中 `EvaluationPage` 为 62.39 kB、gzip 10.80 kB。针对 Playwright 冒烟，发现本机 `3000` 端口被 Docker 中的 `one-api-arm` 占用，导致原有 `e2e/pr13-p1-features.spec.ts` 打到了错误服务，同时该脚本仍使用旧的 `/api/...` 路径；为避免影响现有本机服务，前端 `vite.config.ts` 已改为支持通过 `VITE_PROXY_TARGET` / `VITE_WS_PROXY_TARGET` 配置代理目标，随后以 `backend:3300 + frontend:5174` 组合运行当前项目，并将 `pr13-p1-features.spec.ts` 更新为当前开发环境契约（`/api/v1` + `/api/v2` 分流）。修复后执行 `BASE_URL=http://127.0.0.1:5174 API_URL=http://127.0.0.1:3300 npx playwright test -c playwright.config.e2e.ts e2e/pr13-p1-features.spec.ts`，**6 条冒烟全部通过**。至此 DoD-8、DoD-3、DoD-11 及其相关验收链路均完成收口，当前阶段可标记为“DoD 收口完成，进入下一轮工程化增强”。

### 【已完成】Prompt A/B 效果分析

- [x] **任务 41**：基于真实 Trace 数据增加 Prompt A/B 效果分析接口、前端面板与回归验证。
  - **完成时间**：2026-08-29

  - **结果说明**：新增 `backend/src/services/prompt-ab-analytics.ts`，直接聚合 `traces` 中的 Prompt 版本、状态、延迟与最新评测结果，结合部署基线和 `prompt_ab_variants` 配置生成最近 7/14/30 天的总样本、流量占比、成功率、平均延迟、评测通过率、平均分及每日趋势，不新增迁移、不生成虚假数据；新增 `GET /api/v2/prompt-ab-analytics/:promptId?projectId=&environment=&days=` 接口，完成项目归属、Prompt 与部署校验；`frontend/src/api.ts` 新增分析类型与客户端，`PromptsPage.tsx` 在 A/B 分流面板下新增环境/时间窗口分析面板、变体汇总表与每日趋势表，并在变体保存、删除、环境及窗口切换后自动刷新。新增 `tests/p1-trace-replay-annotation-ab.test.ts` 真实基线/变体 Trace 聚合测试，覆盖样本数、成功率、评测通过率、平均分、配置权重和趋势数据。验证通过：A/B 定向测试、后端 `npx tsc --noEmit`、前端 `npx tsc --noEmit`、前端 `npm run build` 均通过；在 `backend:3300 + frontend:5174` 本地组合环境下补充并执行 Prompt 页面 UI 冒烟，进入“部署”Tab 后可正常看到“A/B 效果分析”及真实 Trace 聚合说明，现有 `pr13-p1-features.spec.ts` 共 **6 条用例全部通过**。

### 【已完成】ESLint 配置与代码质量检查

- [x] **任务 42**：补齐前后端 ESLint 9 配置并完成代码质量检查。
  - **完成时间**：2026-08-29

  - **结果说明**：后端新增 ESLint、`@eslint/js`、`typescript-eslint` 开发依赖与 `eslint.config.mjs`，前端新增 ESLint 9 flat config `eslint.config.mjs`，纳入 TypeScript、React Hooks、React Refresh 检查，并忽略构建产物与 Vite 配置脚本。修复密码特殊字符校验正则、不可变变量声明、WebSocket 发送异常空处理及 Prompt 配置 JSON 解析空 catch 等阻断问题。后端与前端 lint 均执行成功，分别保留 40 条和 12 条非阻断警告；后端/前端 TypeScript 检查、前端生产构建、认证与 Prompt A/B 定向回归测试、`git diff --check` 均通过。

### 【已完成】E2E 全量验收

- [x] **任务 43**：运行并修复 E2E 全量用例，实现 11 个 Playwright 测试文件一次性全绿。
  - **完成时间**：2026-08-29

  - **结果说明**：在 `backend:3300 + frontend:5174` 本地组合环境下执行 `e2e/` 下全部 11 个测试文件共 81 条用例，首轮 77 通过、4 失败。定位后确认为测试脚本与当前实现的契约漂移，并非产品功能回归：`p2-features.spec.ts` P2-5 保存版本后编辑弹窗仍遮挡页面，导致点击“版本历史”超时，修复为先关闭弹窗再切换 Tab；`prompt-journey.spec.ts` Step 4 仍按旧契约直接传 `model:'gpt-4'` 并伪造 `output/latency_ms/status`，当前 Playground 已走真实 Target 调用，修复为先创建模型配置并传 `model_config_id`；`prompt-journey.spec.ts` Step 5 依赖固定 Prompt 名称 `E2E System Prompt`，修复为使用唯一动态 `promptName`；`user-journey.spec.ts` Dashboard 用例把 WebSocket 连接状态的 console 输出误判为页面 JS 错误，修复为改用 `page.on('pageerror')` 仅捕获真正的页面运行时异常，并在断言信息中输出错误明细。修复后重跑全量，`--retries=0` 下 **81 条用例一次性全部通过（5.6 分钟）**，关键冒烟 `pr13-p1-features.spec.ts` 6 条、评测旅程、观测旅程、P1/P2/P3、Prompt 旅程、用户旅程全部通过。

### 【已完成】真实 Agent 接入测试

- [x] **任务 44**：使用 demo-agent 完成真实 Agent 接入联调，验证 SDK 上报链路并修复 ESM 兼容问题。
  - **完成时间**：2026-08-30

  - **结果说明**：以独立项目与真实 API Key（`am_` 前缀，经 `/api/v1/apikeys` 创建）配置 `demo-agent`，构建本地 TS SDK（`sdk`）并安装 demo-agent 依赖后运行完整智能客服 Agent。过程中发现并修复一个真实接入缺陷：`sdk/src/context.ts` 在 Node ESM 环境使用 CommonJS 的 `require('node:async_hooks')`，导致 ESM/`tsx` 下 `ReferenceError: require is not defined`，改为 ESM 命名导入 `AsyncLocalStorage`，Node 与浏览器行为不变。运行 demo 的 6 个场景（订单查询、天气工具调用、故意触发错误、高延迟、LLM Token 统计、决策上报）全部成功并正常结束会话。后端验收：项目统计 `totalTraces=19、totalSessions=1、successRate=94.74%、avgLatency≈193ms、totalTokens=9`；Trace 分类 `message=12、function=4、session=1、llm=1、decision=1`，错误 Trace 为预期的 `failingTool`；`/api/v2/projects/:id/integration-status` 返回 `connected=true`，Hook 覆盖 agent/llm/tool/decision 全部为 true，集成健康检查正常。修复后 SDK 全量单测 **42 条全部通过**，`npm run build` 正常。

  - **接入能力覆盖边界说明**：demo-agent 默认场景验证了 agent/session、message、tool(`wrap`)、LLM(traceLLM+Token)、decision、错误与延迟链路；其中 Retrieval/RAG Hook 默认 demo 未触发，集成诊断提示 `MISSING_RETRIEVAL_SPAN`。随后以真实 API Key 补充一个知识库检索场景（`demo-agent/src/rag-check.ts`：会话 → `withSpan('knowledge_base_search')` 向量检索 → traceLLM 生成 → 结束），运行后 `integration-status` 的 `hooks.retrieval` 由 false 变为 **true**。至此五类接入 Hook（agent / llm / tool / retrieval / decision）均已在真实 Agent 上报下被平台正确识别。

  - **未走真实 Agent 接入、由 E2E/后端单测覆盖的平台能力**（非 SDK 实时上报链路）：评测 Experiment/Run、Worker 队列与 External Runner、Scheduled Run、Trace Sampling 回流、Alert Webhook、Prompt Deployment/A-B Runtime、CLI 门禁、报表导出等。这些在真实 Agent 接入测试中未单独触发，属于平台侧能力而非 SDK 埋点 Hook。

### 【已完成】真实 Agent 驱动的平台全链路闭环测试

- [x] **任务 45**：以真实 Agent 视角驱动平台核心能力端到端闭环，覆盖此前仅由 E2E/单测验证的平台功能。
  - **完成时间**：2026-08-30

  - **结果说明**：新增 `demo-agent/src/closed-loop.ts`（`npm run closed-loop`），用真实 API Key/Service Token 与真实 SDK 上报，串联并断言以下平台能力全链路：① Prompt 创建/版本/**Deployment 部署**/A/B 变体（weight=100）；② **Runtime 解析**（无 bucketKey 走基线、带 bucketKey 命中变体 B，校验 variantKey/version）；③ Service Token（`amt_` + `prompts:read`/`telemetry:write` scope）与数据 Key（`am_`）的正确使用边界；④ 真实 Agent 通过 SDK 完成会话/LLM Trace 上报；⑤ **Trace Sampling 采样回流**（llm 类型过滤，run-now 真实扫描写入 Dataset）；⑥ **commit DatasetVersion** 生成不可变快照；⑦ 创建 **trace\_replay Target**；⑧ 离线 **Evaluator（contains）+ Evaluator Suite**；⑨ 创建三版本齐全 **lifecycle=ready 的 V2 Experiment** 并触发 Run；⑩ Worker 以 claim/lease 执行 **trace\_replay 回放评测**，轮询 Run 到 completed 且 passed/failed 符合预期；⑪ Run 终态触发 **告警规则评估**；⑫ **Webhook 真实 HTTP 投递**（本地 9099 接收端实测收到 `run_completed`）与告警事件台账（`delivery_status=delivered`）。最终 **18 个检查点一次性全部通过**（`sampled=1 → items=1 → Run completed passed=1/failed=0 → webhook delivered`）。

  - 过程中修正了脚本与真实契约的对齐点：Runtime 只读需 Service Token（数据 Key `am_` 用于 V1 遥测）、`run-now` POST 需显式空 JSON body、回流按 `traceTypeFilter='llm'` 聚焦答案 Trace 以保证回放评测输入输出正确。该脚本可对运行中的后端反复执行（`BASE_URL=... npm run closed-loop`），作为平台能力的真实 Agent 回归门禁。

### 【已完成】本地 Docker Compose 部署

- [x] **任务 46**：基于仓库现有 `docker-compose.yml` + Dockerfile 完成本地容器化部署，并修复全新 PostgreSQL 部署无法启动的一系列缺陷。
  - **完成时间**：2026-08-30

  - **部署方式**：三容器编排 —— `postgres:15`（数据卷 `agentmonitor_pgdata`）+ 后端 `node:20-alpine`（Fastify 生产构建，监听 3000）+ 前端 `nginx:alpine`（托管 Vite 构建产物并反代 `/api`、`/ws`、`/health`）。

  - **本地访问地址**：前端 <http://localhost:8080> ；后端直连 <http://localhost:3301> ；PostgreSQL localhost:5432。

  - **新增/调整的部署文件**：

    - 根 `.env`：`JWT_SECRET`（随机 64 位 hex）、`CORS_ORIGINS=http://localhost:8080,http://localhost:5174`、PG 连接参数（本地部署用，勿提交/勿用于生产）。

    - `docker-compose.local.yml`：本地覆盖配置 —— 前端 8080→80、后端 3301→3000、PG 5432 暴露、后端 `DB_SSL=false`（容器内 PG 无 TLS）、挂载本地 nginx 配置。启动命令：`docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build`。

    - `nginx.local.conf`：纯 HTTP 的本地 nginx 配置（生产 `frontend/nginx.conf` 为强制 HTTPS + 自签名证书），使用 Docker 内置 DNS `127.0.0.11` + 变量 `proxy_pass` 动态解析后端，后端未就绪时 nginx 不再启动失败。

    - `frontend/.dockerignore`、`backend/.dockerignore`：排除 `node_modules`/`dist`/`.env`，避免主机（macOS）依赖覆盖容器内（Linux）依赖导致构建失败。

    - `ssl/`：生成本地自签名证书（供生产 compose 的 443 挂载使用，本地 HTTP 方案不依赖）。

  - **修复的全新部署阻断缺陷**：

    1. **后端镜像缺** **`dotenv`**：`dotenv` 误置于 `devDependencies`，生产镜像 `npm ci --only=production` 后运行 `dist/config.js` 报 `ERR_MODULE_NOT_FOUND`。已移到 `dependencies` 并同步 `package-lock.json`。
    2. **后端镜像缺** **`schema.sql`**：`tsc` 不拷贝 `.sql` 资产，迁移模块运行时读取 `dist/db/schema.sql` 报 ENOENT。已在 `backend/Dockerfile` 构建阶段补充 `COPY src/db/schema.sql ./dist/db/schema.sql`。
    3. **PostgreSQL 迁移链从未在全新库跑通**（历史开发均走 SQLite 隐式建表）：① p001 引导迁移建表顺序存在循环依赖（基线 `traces` 外键引用 `prompts`，而 `prompts` 在基线之后才建）——重构为「基线核心表 → prompts/prompt\_versions → ALTER 补 traces 外键 → model\_configs/playground\_runs」的顺序，并在执行基线前剥离 traces 两列的内联外键、建表后用 `DO $$ ... ALTER TABLE ... ADD CONSTRAINT` 幂等补回；② PG 迁移链缺失 **11 张表**（`spans`、`datasets`、`dataset_items`、`dataset_versions`、`evaluators`、`evaluator_versions`、`evaluation_experiments`、`evaluation_results`、`trace_eval_results`、`auto_eval_tasks`、`user_feedbacks`），按 PG 约定（UUID 主键、JSONB 文档列、TIMESTAMPTZ）在 p001 统一补齐；③ 基线 `traces` 缺 `trace_id/span_id/parent_span_id/parent_trace_id/latest_eval_score/latest_eval_passed/updated_at`、`api_keys` 缺 `encrypted_key/expires_at`、`breakpoints` 缺 `hit_threshold/hit_count`、评测相关表缺 `model_config_id/prompt_id/run_config` 等列（SQLite 侧靠运行时 ALTER 补列，PG 路径缺失）——已在 PG DDL 中对齐。

  - **验收结果**：三容器全部 `Up` 且健康检查通过；`/health` 直连与经 nginx 代理均返回 `{"status":"ok"}`；前端首页与 SPA 路由回退 200；经 nginx 代理完成注册→登录→建项目→创建 `am_` 数据 Key 与 `amt_` Service Token→用数据 Key 上报 LLM Trace（返回 trace 且列表可查）→V2 评测接口连通，全链路在 PostgreSQL 持久化下正常。

  - **回归验证**：后端 `tsc` 编译通过；后端单元测试 660+ 用例通过（全量运行时 evaluation-worker/alert-rules 偶发的个别失败为测试套件间共享 SQLite 状态的既有并发抖动，单独运行这两个文件 13/13 全部通过，与本次 PG 迁移/部署改动无关，SQLite 路径行为不变）。

  - **运维命令**：查看日志 `docker compose -f docker-compose.yml -f docker-compose.local.yml logs -f backend`；停止 `docker compose -f docker-compose.yml -f docker-compose.local.yml down`（加 `-v` 同时清空 PG 数据卷）。

### 【已完成】AI 狼人杀游戏智能体接入与监控

- [x] **任务 47**：将 `/Users/lvjianqing/AI_Werewolf_Game_Website` 项目的游戏智能体接入 AgentMonitor 平台并完成监控验证；过程中系统性修复了平台在 PostgreSQL 下的三类兼容缺陷。
  - **完成时间**：2026-08-31

  - **接入方式（纯配置，零改动狼人杀项目）**：狼人杀项目自带 AgentMonitor 兼容 SDK（`api/lib/agentMonitorSdk.ts`），通过每局 `GameConfig.agentMonitor = { enabled:true, baseUrl:'http://localhost:8080', apiKey, agentName }` 开启；AI 走内置 `mock` LLM Provider（无需真实大模型 Key）。在平台创建项目「AI 狼人杀游戏智能体」（ID `b2f24fc9-e3a9-4d80-bab9-a1cfc6247e93`）与数据 Key（`am_lXS9icv...`，名称 `werewolf-game-key`），平台账号 `werewolf@test.com`。跑通一局 9 人全 AI 对局（game ID `25a42dbb-8e0f-4c9e-bbd7-6bac6e43887c`），持续到第 3 天结束。

  - **监控数据验证（PostgreSQL 实查）**：sessions 12、messages 112、traces 115（其中 `llm` 3、`tool_call` 112）、decisions 112（`llm` 决策 3、`rule` 规则决策 109）；`/api/v2/projects/:id/integration-status` 返回 `connected=true`，Hook 覆盖 `llm:true、tool:true`（该游戏无 RAG 检索与 Agent 根 Span，`retrieval/agent` 为 false 属预期）。会话时间线接口 `GET /api/v1/sessions/:id/timeline` 实测返回 114 项（57 消息 + 57 Trace），`input/output/metadata` 均正确解析为 JSON 对象。探针脚本 `demo-agent/src/ww-probe.ts` 直接调用狼人杀 SDK，5 类埋点（ensureSession/trackMessage/traceLLM/trackDecision/trackToolCall）全部上报成功。

  - **修复的平台 PostgreSQL 兼容缺陷**（此前开发始终走 SQLite，PG 路径从未被完整覆盖）：

    1. **telemetry 业务 ID 类型不兼容**：狼人杀 SDK 使用人类可读字符串会话/决策 ID（如 `game:<gid>:seat:<n>:<role>`、`decision_<uuid>`），而 PG 的 `sessions.id/decisions.id/decision_options.id` 等为 UUID 类型，写入报 `22P02 invalid input syntax for type uuid`。新增迁移 **p014\_telemetry\_id\_varchar**：PG 下先删除引用外键，将 `sessions.id / decisions.id / decisions.session_id / decision_options.id / decision_options.decision_id / messages.session_id / traces.session_id / tool_calls.session_id / spans.session_id` 由 UUID 放宽为 `VARCHAR(255)`，再重建外键（SQLite 侧 TEXT 天然宽容，为空操作）。
    2. **JSONB 双重序列化/双重解析**：服务层对 JSON 列统一 `JSON.stringify` 写入，PG 的 JSONB 由 `node-postgres` 自动序列化，导致写入 `"[object Object]"`；读取侧直接 `JSON.parse(对象)` 也会失败。在 `db/index.ts` 新增跨库归一化辅助 `toDbJson`（SQLite 转字符串 / PG 传对象）、`fromDbJson`（读取归一）、`toDbBool`（SQLite 绑定 0/1 / PG 绑定布尔）、`fromDbBool`（读取归一）与 SQL 布尔字面量常量 `SQL_TRUE/SQL_FALSE`，并重构 session/trace/toolCalls/decision/trace-sampling/scheduled-run/run-reports/prompt-ab-analytics/trace-evaluation/evaluation/evaluation-run/alert-rule/trace-annotation/evaluator-suite/agent-target/breakpoint/prompt-ab-test 等服务的写入与读取路径。
    3. **布尔列与整数比较/绑定**：SQL 中 `enabled = 1`、`passed = 0` 这类 SQLite 整数布尔写法在 PG 报 `operator does not exist: boolean = integer`，后台 evaluation-worker 的 schedule/sampling 定时器持续报错；统一改为跨库 `= ${SQL_TRUE}/${SQL_FALSE}`，写入布尔列统一走 `toDbBool`（better-sqlite3 不能绑定 JS 布尔，PG 不能绑定整数）。
    4. **tool\_calls 列名差异**：PG `tool_calls` 表输入列为 `input_params`（SQLite 历史表为 `input`）且 PG 无 `status` 列，时间线查询 `column "input" does not exist` 报 500。`toolCalls.ts` 与 `session.ts` 时间线查询按 `config.dbType` 切换列名/别名（`input_params AS input`，PG 下 status 取常量 `'completed'`）。
    5. 附带修复 `createSession` 的幂等写入在 SQLite 分支误用 `?` 占位符（与抽象层 `$N→?` 转换冲突导致 `Too few parameter values`），统一为 `$N` 占位。

  - **回归验证**：后端 `tsc --noEmit` 通过；后端单元测试 **666/667 通过**（剩余 1 个为 evaluation-worker/runner 在全量并行套件下的既有并发时序抖动，相关文件单独运行均全绿，与本次改动无关）；`db-migrations.test.ts` 已同步补充 p014 迁移断言。Docker 后端容器重建后，PG 迁移成功、worker 连续多个 schedule/sampling 轮询周期 **零** **`boolean = integer`** **报错、近 40 秒 500 错误为 0**。

### 【已完成】代码质量与架构优化专项（分析建议全量落地）

- [x] **任务 48**：将代码审查发现的优化建议全部落地，并完成四类测试回归。
  - **完成时间**：2026-09-05

  - **结果说明**：

    - **成本口径统一 + 价格表外置**：修复 `services/cost.ts` 中成本汇总/趋势在 SQL 内写死单价 `0.001/0.002`（default 档）与按模型真实单价（getModelPricing）双轨不一致的问题；汇总、按模型分布、TOP 贵调用、趋势全部改为「SQL 聚合 Token → JS 层按模型真实单价 `calculateCost` 计费」，口径一致。价格表扩展至 gpt-4.1/o1/o3、claude-3.5/4、gemini、deepseek、qwen、glm 等，并支持 `MODEL_PRICING_JSON` 环境变量覆盖/扩展私有模型单价；成本优化建议改为机器可读 `code`（downgrade\_gpt4/downgrade\_opus/enable\_caching/shorten\_prompts）便于前端 i18n。

    - **日志脱敏**：`db/sqlite.ts` 新增 `redactParams`/`logSqlError`，SQL 异常日志对敏感键（key/token/secret/password/hash/authorization/content/prompt）脱敏、长字符串截断，生产环境仅打印 SQL 摘要，杜绝 Prompt/密钥随异常入日志。

    - **遥测批量接口加固**：`POST /api/v2/telemetry/batch` 增加路由级 `bodyLimit=10MB`（默认 1MB 导致大批量 Span 上报 413）与单批 1000 事件上限（超限 413 并返回 maxEvents）。

    - **统计性能**：新增迁移 **p015\_stats\_performance\_indexes**，为 traces/sessions 补齐 `(project_id, started_at DESC)`、`(project_id, status)`、`(project_id, trace_type)`、`(project_id, started_at)` 复合索引（SQLite 侧 initSqliteSchema 同步创建）；`services/stats.ts` 为 getProjectStats/getObservationStats 增加 10s TTL 缓存（并发去重、失败不缓存），缓解 Dashboard 15s 轮询的全表聚合压力。

    - **PG 连接池配置生效**：`db/pool.ts` 改为读取 `config.database.poolMax/poolIdleTimeout`（对应 `DB_POOL_MAX`/`DB_POOL_IDLE_TIMEOUT`），不再写死 max=20。

    - **WebSocket 安全与性能**：`routes/ws.ts` 重写鉴权，凭据优先级 `Authorization 头 → Sec-WebSocket-Protocol(bearer.<token>) → query token（兼容旧客户端）`，并支持 Service Token（`amt_`，自动订阅所属项目、不占用户连接配额）；广播改为按 `clientsByProject` 索引分发，避免全量遍历；心跳超时清理同步维护索引。

    - **弱密钥告警**：`config.ts` 在非生产环境未设置 `JWT_SECRET` 时启动显著告警。

    - **前端请求层统一**：删除简陋且行为不一致的 `hooks/useApi.ts`，`DecisionMonitor.tsx` 改走统一 `api` 客户端（新增通用 `api.get`，自动带鉴权头/401 刷新/GET 去重/重试）；同时为决策轮询加入请求序号竞态保护（仅采纳最新结果）。

    - **构建分包**：`vite.config.ts` 增加 `manualChunks`，将 react/react-router、recharts、lucide-react 拆为独立 vendor chunk（chart-vendor 420KB、react-vendor 49KB 独立缓存）。

    - **顺带修复的既有缺陷**：① `services/session.ts` 会话时间线在 SQLite 下误用 PG 的 `input_params` 列名导致 tool\_call 时间线 500（任务 47 曾修 toolCalls.ts 但遗漏 session.ts），已按 dbType 切换列名/别名；② agent 测试 helper 密码 `Test123456!`（11 位）不满足 12 位策略导致注册 400，已改为 `Test12345678!`；③ agent 测试多处 baseUrl/WS\_URL 硬编码 `localhost:3000`，统一改为由 `API_URL` 环境变量派生，WS 握手补齐 JWT query token。

  - **回归验证（全部通过）**：

    - 后端 `tsc --noEmit` 通过；后端单元测试 **65 文件 667 测试全部通过**（`vitest run --no-file-parallelism`）。

    - 前端 `tsc --noEmit` 通过；前端单元测试 **7 文件 69 测试全部通过**；`npm run build` 成功（vendor 分包生效）。

    - 前后端 `eslint` 均 **0 errors**（仅保留既有非阻断 warning：后端 41、前端 11）。

    - **Agent 测试**（tests/agent，后端起于 3377）：**8 文件 25 测试全部通过**。

    - **E2E 测试**（Playwright，前端 5174 代理后端 3377）：**81 条用例全部通过（5.6 分钟，--retries=0）**。

### 【已完成】上线前生产就绪修复（无域名 + 自签证书 + IP 访问）

- [x] **任务 49**：修复上线评审发现的生产部署阻塞项，并补齐无域名场景下的 HTTPS 自签证书链路。
  - **完成时间**：2026-09-05

  - **结果说明**：

    - **自签证书支持 IP 访问**：重写 `scripts/ssl-setup.sh` 的自签逻辑，新增 `detect_lan_ips` 自动探测本机局域网 IP，自签证书 SAN 默认包含 `localhost / 127.0.0.1 / 探测到的全部局域网 IP`，并支持 `--ip <ip>`（可重复）追加公网 IP；改用 openssl `-config` + `alt_names`（DNS.n/IP.n）方式生成，兼容多 SAN，有效期 825 天。已实测生成证书 SAN 正确（`DNS:localhost, IP:127.0.0.1, IP:192.168.43.62, IP:<自定义>`）。

    - **前端容器暴露 443**：`frontend/Dockerfile` 增加 `EXPOSE 443`（80 用于 HTTP→HTTPS 跳转，443 承载 HTTPS）；现有 `nginx.conf` 已具备 80→443 跳转、`/api`/`/ws` 反代、安全头，自签场景通用，无需改动。

    - **修复生产数据库连接失败隐患（P0）**：`docker-compose.yml` 的 backend 服务显式设置 `DB_SSL="false"`——容器内通过 Docker 内网连接自带 PostgreSQL（无 TLS），而 config 生产默认 `DB_SSL=true` 会导致连库失败；并注明改用云端带 TLS 托管库时置 true。

    - **消除弱密码/弱密钥默认值（P0）**：PostgreSQL 密码与 JWT 密钥改为 `${PG_PASSWORD:?...}` / `${JWT_SECRET:?...}` 强校验，未在 `.env` 设置时 `docker compose config/up` 直接报错拒绝启动；PG 健康检查改用 `PG_USER` 变量。

    - **限流生产阈值收敛（P0）**：backend 环境显式 `RATE_LIMIT_MAX`（默认 100），防止把本地测试用的超大阈值带入生产导致限流失效。

    - **新增生产配置模板** **`.env.production.example`**：含 JWT/PG 强密钥生成命令、按 IP 的 `CORS_ORIGINS=https://<IP>` 填写示例、DB\_SSL/限流/连接池/模型单价覆盖等说明，引导无域名 IP 访问配置。

    - **文档同步**：`.env.example` 补全 DB\_SSL 与限流生产说明；`docs/生产部署指南.md` 部署步骤改为使用生产模板并加入自签证书步骤、新增「4.1 无域名 + 自签证书 + IP 访问」专节，迁移台账说明更新为 p001\~p015。

  - **验证结果**：`docker compose config` 解析通过；空环境模拟缺失必填变量时 compose 正确报错（PG\_PASSWORD/JWT\_SECRET 强校验生效）；`.env.production.example` 与 `ssl/` 目录 gitignore 状态正确（模板可提交、证书/真实 .env 被忽略）；ssl-setup.sh 实际生成证书 SAN/有效期校验通过。本次仅改动部署/脚本/文档，未改动前后端业务代码，既有测试结论不受影响。

### 【已完成】生产环境首次部署（云服务器 14.103.80.198）

- [x] **任务 50**：将 AgentMonitor 部署到云服务器并通过 HTTPS 提供服务（无域名 + 自签证书 + IP 访问）。
  - **完成时间**：2026-09-05

  - **部署环境**：Ubuntu 24.04 LTS / 2 vCPU / 3.8GB RAM / Docker 29.7 + Compose v5.5；部署目录 `/opt/agentmonitor`；SSH ControlMaster 长连接（ServerAliveInterval=30）。

  - **端口避让**：同机 80 端口已被另一业务容器 `styleagent-frontend` 占用且在运行，未做改动；AgentMonitor 前端 **HTTPS 直接使用 443**，HTTP 跳转端口映射到主机 **8080**（容器内仍 80→443 跳转）。为此把 `docker-compose.yml` 前端端口参数化为 `${FRONTEND_HTTPS_PORT:-443}` / `${FRONTEND_HTTP_PORT:-80}`。

  - **部署步骤**：本地打 tar 包（排除 node\_modules/dist/.git/data/.env/.deploy，约 966KB）→ scp 上传 → 解压 → 在服务器生成 `.env`（`openssl rand` 生成 JWT\_SECRET 与 PG\_PASSWORD；`CORS_ORIGINS=https://14.103.80.198`；`RATE_LIMIT_MAX=100`）→ `bash scripts/ssl-setup.sh --self-signed --ip 14.103.80.198` 生成含公网 IP SAN 的自签证书 → `docker compose build && docker compose up -d`。

  - **验证结果（全部通过）**：

    - 三容器 `agentmonitor-postgres/backend/frontend` 全部 healthy/Up；backend 日志显示 **`PostgreSQL migrations completed`**（全新 PG 库跑通 p001\~p015 迁移）、`Server listening :3000`、evaluation-worker 已启动。

    - 外网 `https://14.103.80.198/health` 返回 `{"status":"ok"}`；前端首页 `https://14.103.80.198/` 返回 200（SPA）。

    - 通过 HTTPS 调用 `POST /api/v1/auth/register` 成功返回 token（验证 nginx `/api` 反代 + 后端 + PostgreSQL 全链路）。

    - 服务器本地 `http://127.0.0.1:8080/` 返回 301 跳转到 `https://`；`https://127.0.0.1/health` 返回 200。

  - **访问说明**：浏览器打开 \*\*<https://14.103.80.198/**，自签证书首次访问会提示「不安全」，手动「继续访问/信任证书」即可；已用> `admin@agentmonitor.local / Admin@2026Secure` 注册验证账号（可作为首个管理员，建议登录后按需修改）。

  - **运维命令**（在 `/opt/agentmonitor`）：`docker compose ps`、`docker compose logs -f backend`、`docker compose restart`、`docker compose down`（加 `-v` 会清空 PG 数据卷）；更新代码后重新上传并 `docker compose up -d --build`。

### 【已完成】设置页 SDK 下载入口 + 自签证书跳过 TLS 校验指引（方式二）

- [x] **任务 51**：在设置页提供三语言 SDK 安装包下载链接，并给出自签证书场景下跳过 TLS 校验（方式二）的接入指引。
  - **完成时间**：2026-09-05

  - **SDK 安装包**：将 TypeScript/Node.js、Python、Go 三个 SDK 打包为 tarball 放入 `frontend/public/sdks/`（Vite 构建时自动映射到站点根路径 `/sdks/`，nginx 静态直出）：
    - `agentmonitor-sdk-typescript.tar.gz`（约 49KB，含 `dist/` 构建产物、`src/`、`package.json`）
    - `agentmonitor-sdk-python.tar.gz`（约 16KB，含 `agentmonitor/` 包、`setup.py`，已排除 `__pycache__`）
    - `agentmonitor-sdk-go.tar.gz`（约 22KB，含 `agentmonitor/` 包、`go.mod`）

  - **页面改动**：
    - 新增组件 [SdkDownloads.tsx](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/components/SdkDownloads.tsx)：三个下载按钮（`<a download>` 直链）、服务 base URL 展示（取 `window.location.origin`，部署后自动显示 `https://14.103.80.198`）带一键复制、TS/Python/Go 三个 Tab 的安装接入代码块（解压→构建/安装→初始化→上报示例）、琥珀色警示区展示**方式二：跳过自签证书 TLS 校验**的各语言配置代码；所有代码块带复制按钮。
    - [SettingsPage.tsx](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/pages/SettingsPage.tsx) 在项目/API Key 卡片网格下方全宽挂载该卡片。
    - [i18n.ts](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/i18n.ts) 新增 `sdkDownloads / sdkDownloadsDesc / sdkDownload / sdkInstall / sdkBaseUrl / sdkTlsTitle / sdkTlsDesc` 中英文案。

  - **方式二（跳过 TLS 校验）各语言方案**：
    - Node.js/TS：运行前 `export NODE_TLS_REJECT_UNAUTHORIZED=0`（SDK 基于原生 `fetch`，受该环境变量控制）。
    - Python：SDK 基于 `requests`，在 `init` 前对 `requests.Session.request` 打补丁强制 `verify=False`。
    - Go：SDK 使用 `http.DefaultTransport`，程序启动时设置 `http.DefaultTransport.(*http.Transport).TLSClientConfig = &tls.Config{InsecureSkipVerify: true}`。

  - **验证结果（全部通过）**：
    - 本地 `npm run build` 成功、`tsc --noEmit` 0 错误，`dist/sdks/` 三个包均在产物中。
    - 上传变更源码到 `/opt/agentmonitor`，`docker compose build frontend` 成功并 `up -d` 重建容器。
    - 外网实测 `https://14.103.80.198/sdks/agentmonitor-sdk-{typescript,python,go}.tar.gz` 均返回 **HTTP 200**、`application/octet-stream`，字节数与本地一致（50193/15874/21992），下载包可正常解压。
    - 线上 Settings 页面 chunk（`SettingsPage-DNuDfy-9.js`）已包含下载文件名与 `NODE_TLS_REJECT_UNAUTHORIZED` 等新内容。

### 【已完成】设置页 API 调用文档（HTTP 直连接口说明 + curl 示例）

- [x] **任务 52**：在设置页提供对外 HTTP API 调用文档，方便用户不集成 SDK 时直接对接上报。
  - **完成时间**：2026-09-05

  - **页面改动**：
    - 新增组件 [ApiDocs.tsx](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/components/ApiDocs.tsx)，挂在设置页 SDK 下载卡片下方，包含：base URL 展示、鉴权方式说明、接口列表表格、可复制的 curl 示例（代码块均带复制按钮）。
    - [SettingsPage.tsx](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/pages/SettingsPage.tsx) 引入并全宽挂载。
    - [i18n.ts](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/i18n.ts) 新增 `apiDocs / apiDocsDesc / apiAuth / apiAuthDesc / apiEndpoints / apiExample / apiV2Example / apiRespExample` 中英文案。

  - **文档内容（依据后端实际实现核对）**：
    - **鉴权**：所有遥测接口走 `apikeyMiddleware`，API Key（`am_` 开头）支持 `X-API-Key` 请求头或 `Authorization: Bearer` 两种方式任选。
    - **接口列表**：`POST /api/v1/traces`（单条 Trace，traceType/name 必填）、`POST /api/v2/telemetry/batch`（批量事件，单批 ≤1000，202 返回 receipts）、`POST /api/v1/spans`、`POST /api/v1/feedbacks`（rating 仅 1/0/-1）、`POST /api/v1/snapshots`、`GET /api/v1/breakpoints/public`、`PATCH /api/v1/traces/:id`、`GET /api/v1/sessions/:id`、`GET /ws`（WebSocket，token 经 query/子协议）。
    - **curl 示例**：v1 上报 LLM Trace、v2 批量上报（schemaVersion=2.0、eventType=trace.end、payload 包裹）、反馈与 wss 订阅；示例统一加 `-k` 说明用于跳过自签证书校验。

  - **验证结果（全部通过）**：
    - 本地 `tsc --noEmit` 0 错误、`npm run build` 成功。
    - 已部署并重建前端容器；线上 Settings chunk 含 `apiDocs`、`X-API-Key`、`v2/telemetry/batch` 内容。
    - 外网实测：无 key / 错误 key 调 `/api/v1/traces` 均返回 **401**（鉴权与路由生效）；三个 SDK 下载链接仍 200。
    - **端到端真实调用**：登录 → 建项目 → 建 API Key → `POST /api/v1/traces` 成功返回 trace 对象（id）；`POST /api/v2/telemetry/batch` 返回 **202** `{"summary":{"total":1,"accepted":1,"rejected":0}}`。验证后已删除测试项目（级联清理临时 Key 与数据，返回 204）。

### 【已完成】设置页 SDK 下载 / API 接入两板块改为折叠面板

- [x] **任务 53**：将设置页「SDK 下载」与「API 调用文档」两个板块改为可折叠面板，默认收起，点击标题展开/收起，避免设置页过长。
  - **完成时间**：2026-09-06

  - **改动内容**：
    - [SdkDownloads.tsx](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/components/SdkDownloads.tsx) 与 [ApiDocs.tsx](file:///Users/lvjianqing/agent-monitor/AgentMonitor/frontend/src/components/ApiDocs.tsx) 各新增 `expanded` 折叠状态（`useState(false)` 默认收起）；标题区改为可点击 `<button>`（标题 + 描述 + lucide `ChevronDown/ChevronUp` 箭头），正文区用 `{expanded && <>...</>}` 条件渲染，展开内容顶部加 `mt-4` 间距。

  - **验证结果**：本地 `tsc --noEmit` 0 错误、`npm run build` 成功；已部署重建前端，线上 Settings chunk 含 `useState(!1)`（默认收起）与折叠样式；三个 SDK 下载链接仍 200。



